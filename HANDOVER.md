# ART 한글화 — 인계 문서 (2026-08-24, 8차 세션)

> **새 세션에 줄 첫 지시:**
> "`C:\Users\user\.claude-parallel\art-korean\HANDOVER.md` 를 읽고 이어서 작업해줘."
>
> 파일을 따로 업로드할 필요가 없습니다. 필요한 것 전부(원본 PDF까지) 이 폴더에
> 로컬로 들어 있습니다. **지난 인계는 파일이 다른 머신에 있어서 깨졌습니다.**
> 이번에는 같은 머신의 이 경로에 다 모아뒀습니다.

> **읽는 순서:** `0-D`(최신) → `0-C` → `0-B` → `0-A`(5차) → `0-0`(4차) → `0`(2~3차) → `1`~`9`(불변 규칙).
> 앞 절이 항상 더 최신입니다. 뒤 절의 숫자는 그 세션 시점 기록이니 그대로 믿지 마십시오.
> 실측이 필요하면 `python tools/gaps.py`, `node tools/test.js` 를 돌리십시오.

---

## 0-E. 8차 세션 (2026-08-24 저녁) — 원거리 갈래 복구 완료 + **계정 3개 병렬 순회 장치**

### 이번에 끝낸 것

| 항목 | 결과 |
|---|---|
| 간선 그래프 | 443 → **813** (근거리 341 / 원거리 370), 미탐색 0 |
| 화면 | 81 → **82** (`q048_dispersion.aspx` 새로 발견) |
| 문구 수집 | **199/199 조합 완료** (`crawl_labels.json`) |
| 사전 | 2,084 → **2,086** |
| 빌드 | **v1.6.3**, `node test.js` **67/67 통과** |

**사용자는 템퍼몽키를 v1.6.3 으로 갱신해야** 아래 두 문구가 한글로 보입니다.

### 뿌리 원인: 문맥(ctx) 키에 근거리/원거리가 없었습니다

7차까지 `ctx` 는 제품 유형만 담았습니다. 그래서 **근거리로 도착한 화면과
원거리로 도착한 화면이 같은 키**가 되어, 먼저 도착한 쪽(근거리)만 자식 갈래를
열거하고 끝났습니다. 원거리에서만 나오는 격리(q043)·개인 밀폐(q044) 화면이
그래프에서 통째로 빠져 있었습니다.

- 실측 증거: 경로에 `q016=rbYes` 인 간선 **346개**, `rbNo` 는 **5개**뿐
- 고친 곳: `tools/crawl.js` 의 `CTX_SCREENS = ['q016_nearfieldsource']` + `ctxOf()`
- **다시 순회하지 않고** `tools/migrate_ctx.py --write` 로 옮겼습니다 —
  간선마다 `path` 가 통째로 남아 있어 계산으로 재키잉이 됩니다. 충돌 0건.

### 새로 메운 번역 구멍 2개

| 문구 | 왜 새어나갔나 |
|---|---|
| `Select Personal Enclosure` → `격리(작업자) 선택` | 짝인 `Select Segregation` 만 사전에 있었음. `Personal Enclosure` 만 부분 치환돼 화면에 **`Select 격리(작업자)`** 로 떴습니다 |
| `Activity Subclass/Example Activities` → `활동 하위등급/예시 활동` | 두 낱말은 따로 있었지만 슬래시로 붙은 형태가 없었음 |

### **`gaps.py` 는 이런 걸 못 잡습니다 — 아직 안 고쳤습니다**

`crawl.js` 의 `ignorable()` 에 `if (HANGUL.test(t)) return true;` 가 있습니다.
**한글이 한 글자라도 섞이면 구멍으로 수집되지 않습니다.** 그래서 위의
`Select 격리(작업자)` 같은 반쪽 번역은 기존 도구로 영원히 안 잡힙니다.

대신 이번에 `tools/audit_labels.py` 를 만들었습니다. 화면에 보이는 글만 골라
(라디오 값 `rbLiquids`·컨트롤 이름 `ctl00$...` 는 제외) 영어만/섞임/한글만으로
가릅니다. **다음 세션은 `ignorable()` 을 손대든지, audit_labels.py 를
정규 점검에 넣든지 결정해야 합니다.**

```
python tools/audit_labels.py          요약
python tools/audit_labels.py --mixed  섞인 문구 전부
```

현재 상태: 화면 문구 318개 = 한글만 222 / 섞임 49 / 영어만 42 / 그 외 5.
영어만 42 중 **단위 11 + 수집 타이밍 문제 30 + 진짜 구멍 1(메움)**.
섞임 49 는 전부 확인했고 `ART`(고유명사)와 단위(`kg/minute`, `m²/hour`, `ACH`)뿐입니다.

**남은 판단거리:** 단위 표기가 `m2` 와 `m²` 로 섞여 있습니다. 원문 자체의
흔들림이라 통일할지는 사용자가 정해야 합니다. 아직 안 물어봤습니다.

---

### 계정 3개 병렬 순회 — 장치는 다 만들었고 **계정 연결만 남았습니다**

사용자 지시: *"중복은 감당해야해. 계정 3개 있으니깐 가능하고, 검증 차원에서
몇 번 더 순회 돌릴거야."* → 경로 중복 제거 최적화는 **하지 않기로 했습니다.**

#### 왜 프로필을 갈라야 하는가 (같은 계정으로 창만 늘리면 안 됩니다)

ART 는 ASP.NET WebForms 라 **마법사가 몇 번째 화면인지를 서버가 세션에**
들고 있고, 세션은 로그인 쿠키 하나에 붙습니다. A 가 7단계까지 걸어간 사이
B 가 처음으로 되돌리면, A 는 자기가 아직 7단계인 줄 알고 다음을 눌러
**엉뚱한 화면의 결과를 자기 기록으로 적습니다.** 예외가 안 나므로 조용히 틀립니다.

> 이번 세션에 제가 실제로 이 사고를 냈습니다. 문법 확인한다고
> `node -e "require('./labels.js')"` 를 돌린 게 **두 번째 일꾼**이 되어
> 돌던 순회의 세션을 리셋했고 `[174/178] 실패: 경로 이탈` 이 찍혔습니다.
> **문법 확인은 `node --check` 로만 하십시오.** (그 조합은 다시 채웠습니다.)

쿠키는 크롬 프로필(`user-data-dir`)마다 따로입니다. 프로필을 나누고 계정을
달리 로그인하면 세션이 완전히 갈립니다.

#### 만든 것

| 파일 | 하는 일 |
|---|---|
| `tools/parallel.ps1` | 프로필 복제·기동·정지, 병렬 실행 (아래 명령들) |
| `tools/merge_labels.py` | `crawl_labels.s*.json` 조각들을 하나로 합침. 같은 키인데 내용이 다르면 **덮어쓰지 않고 목록으로 보여줍니다** |
| `tools/compare_edges.py` | 독립 순회 결과들을 맞대 봄 (한쪽에만 있는 간선 / **도착지가 다름** / 판정 엇갈림) |
| `tools/audit_labels.py` | 위 참조 |
| `tools/progress.py` | 진행 막대바 (`--watch`) |

바꾼 곳:
- `tools/cdp.js` — `ART_PORT`(기본 9222) 추가, `module.exports` 에 `PORT`
- `tools/labels.js` — `ART_SHARD=1/3` 분할, 결과는 `crawl_labels.s1.json` 등으로 **따로** 씀
  (한 파일에 같이 쓰면 나중에 저장한 쪽이 앞사람 것을 통째로 덮어씁니다)
- `tools/crawl.js` — `ART_RUN=va` 면 `crawl_edges.va.json` 등 **파일 일습이 통째로** 갈림

#### 명령

```powershell
cd C:\Users\user\.claude-parallel\art-korean\tools
.\parallel.ps1 status    # a=9222 / b=9223 / c=9224 상태
.\parallel.ps1 setup     # 디버그 크롬 전부 닫고 실행. a 프로필 복제 → b, c
.\parallel.ps1 start     # b, c 기동  → 창마다 **다른 계정으로 로그인**
.\parallel.ps1 labels    # 문구 수집 3등분 동시 실행
.\parallel.ps1 verify    # 계정마다 처음부터 독립 순회
.\parallel.ps1 stop      # b, c 만 닫음
```

`setup` 은 프로필을 통째로 복사하므로 **템퍼몽키와 한글화 스크립트가 그대로
따라옵니다.** 쿠키(`Default\Cookies`, `Default\Network\Cookies`)만 지워 로그인
화면부터 시작하게 합니다. 사용자가 평소 쓰는 크롬은 건드리지 않습니다 —
`user-data-dir` 이 `.chrome-debug*` 인 것만 고릅니다.

#### `verify` 가 이번 병렬의 진짜 목적

셋이 아무것도 공유하지 않고 각자 처음부터 돕니다. 끝나면:

```
python compare_edges.py crawl_edges.va.json crawl_edges.vb.json crawl_edges.vc.json
```

이 순회는 오류 없이 조용히 틀릴 수 있습니다(7차 때 거짓 간선 141개, 예외 0건).
**셋이 같은 자리를 다르게 적었다면 거기가 의심할 자리**입니다.
`compare_edges.py` 는 '도착지가 다름' 을 제일 위험한 것으로 따로 뽑습니다.

### 다음 세션이 할 일 (순서대로)

1. **사용자에게 요청**: 디버그 크롬 닫기 → `.\parallel.ps1 setup` → `start`
   → 두 창에 나머지 두 계정 로그인 → `.\parallel.ps1 status` 로 `ART 탭` 확인
2. `.\parallel.ps1 verify` 실행. 오래 걸립니다 — `python progress.py --watch verify.a.log`
3. 끝나면 `compare_edges.py` 로 맞대 보고, **어긋난 자리를 사람이 직접 확인**
4. `m2` / `m²` 표기 통일 여부를 사용자에게 묻기
5. `gaps.py` 의 한글 맹점 처리 결정
6. 흐름도 재생성: `python flow.py` → `flow_one.py` → `lint_mmd.py` → `flow_page.py`
   → `tools/flow/art-flow.html` 을 기존 아티팩트에 재배포
   (`https://claude.ai/code/artifact/e789286a-f01e-4b20-8d60-9aaba309d39a`)
7. 마무리로 `python tools/pipeline.py`

### 7차에서 넘어온 미해결 (그대로 남아 있습니다)

- 포스트백 전용 UI: 활동 이름/지속시간 편집, 활동 2~4 슬롯
- 액체 시나리오 결과/베이지안 실행

### 손대지 않는 것

`register.aspx`, 로그인 실패, 비밀번호 재설정 — 계정에 영향이 갑니다.
`probe_all.js` 는 '다음' 만 누릅니다. 삭제·로그아웃·저장·완료는 누르지 않고
`confirm` 은 항상 false 를 반환합니다.

---

## 0-D. 7차 세션 (2026-08-24) — 질문 흐름도 + **자동화가 조용히 거짓을 적고 있었습니다**

**이 절이 가장 최신입니다.** 이번 세션의 요청은 번역이 아니라 "ART가 무엇을 순서대로
묻는가" 를 흐름도로 만드는 것이었습니다. 만드는 과정에서 **지난 세션들의 수집
데이터에 거짓이 섞여 있던 것**을 찾았습니다.

### 최종 숫자 (실측, 2026-08-24)

| 항목 | 값 | 확인 방법 |
|---|---|---|
| 사전 | **2,082개** (고유 키 2,080 — 중복 2개는 번역이 같아 무해) | `python tools/gaps.py` |
| 빌드 | `@version 1.6.1`, **277,854자** (371,595바이트) | `wc -c art-korean.user.js` |
| 테스트 | **67/67 통과** | `cd tools && node test.js` |
| 미번역 | **0개** | `tools/gaps.json` 비어 있음 |
| 범위 밖 (이유 기록) | 66개 | `tools/crawl_outofscope.json` |
| 간선 | **443개** — 확인됨 414 / 막힘 21 / 선택불가 8 / **미탐색 0** | `tools/crawl_edges.json` |
| 방문 화면 | 81개 (마법사 65 + 그 밖 16) | `tools/crawl_screens.json` |
| 문구 수집 | 108개 (화면@맥락), 미확인 0 | `tools/crawl_labels.json` |
| 상호작용 탐침 | 61개 화면 | `tools/probe_done.json` |

> **주의:** 이전 절들이 빌드 크기를 "277,606 chars" 로 적었습니다. 그것은 **글자 수**
> 입니다. 바이트로는 37만이 넘습니다. 한글이 UTF-8 에서 3바이트라 그렇습니다.
> 크기가 갑자기 커진 것처럼 보이면 이것을 먼저 의심하십시오.

### ★★ 가장 중요 — 잠긴 컨트롤 (`disabled`)

ART는 제품 유형 8개 중 **3개를 항상 잠가 놓습니다**: `rbHotMetal`(고온·용융 금속),
`rbFibrous`(섬유상 물질), `rbGas`(가스). 물질을 톨루엔으로 바꿔 봐도 그대로입니다
(`node tools/probe_cas.js 108-88-3` 로 확인). 이유는 ART가 스스로 밝혀 둡니다:

> ART는 현재 흡입성 분진·증기·미스트만 평가하도록 보정되어 있습니다. 다만 적절한
> 보정 데이터가 없어, 흄·섬유·가스 및 고온 금속 공정의 배출로 생기는 분진은
> 당분간 평가할 수 없습니다.

**무엇이 잘못됐었나.** `pagelib.js` 의 `pickRadio()` 가 잠긴 라디오를 `click()` 하고
`'OK'` 를 반환했습니다. 잠긴 라디오는 클릭해도 아무 일이 없습니다. 순회기는 성공했다고
믿고 `다음` 을 눌렀고, **원래 골라져 있던 제품 유형의 동작을 그 세 갈래의 기록으로
적었습니다.** 증거: 가스·섬유상의 도착지가 페이스트·슬러리와 같았고, 고온금속의
도착지가 분말·과립과 같았습니다 — 그때 체크돼 있던 값에 따라 갈렸습니다.

| | |
|---|---|
| 지운 거짓 간선 | **141개** |
| 백업 | `tools/crawl_edges.pre-disabled.json` |

**고침:** `pickRadio`/`pickSelect` 가 잠긴 컨트롤에 `'DISABLED'` 를 반환하고,
`crawl.js`·`labels.js` 가 그걸 보면 `dest='DISABLED'` 로 확정하고 멈춥니다.

**다른 도구에도 적용되는 교훈:** 컨트롤을 조작하는 함수는 **조작이 실제로 반영됐는지
확인**해야 합니다. `click()` 을 불렀다는 것과 값이 바뀌었다는 것은 다른 얘기입니다.
Stoffenmanager·ECETOC 로 넓힐 때 같은 함정이 있습니다.

### 함께 고친 구조적 결함 4개

| 결함 | 증상 | 고침 |
|---|---|---|
| 실패한 간선을 무한 재선택 | 배경 순회가 **같은 자리에서 209번** 헛돎 | 간선별 실패 3회 → `dest='UNREACHABLE'` |
| `활동 구성` 이 이전 활동을 **이어받아** 중간 화면에서 시작 | 경로 되밟기가 첫걸음부터 어긋남, 문구 수집 실패율 50% | `reset()` 이 `이전` 을 눌러 `q003_090_producttype` 까지 되돌림 |
| 선택지 없는 화면(숫자만 입력)을 막다른 길로 취급 | **액체 갈래 전체가 그래프에 없었음** | `choices()` 가 `{kind:'linear'}` 갈래 1개를 만들어 통과 |
| CSS 계단 순서 — `@media` 를 앞에 두고 뒤에서 덮음 | 게시한 페이지에서 목차가 본문 위에 겹쳐 뜸 | 반응형 규칙을 스타일시트 **맨 끝**으로 |

세 번째 고침의 효과: **액체 갈래 간선 4개 → 105개, 화면 2개 → 28개.**
그리고 그 안에서 **미번역 2개**가 나왔습니다 (`q013_100_viscosity`):
`Viscosity` → 점도, `Select the viscosity of the substance/preparation…` → 물질/조제품의
점도를 선택한 뒤 '다음'을 클릭해 계속하십시오. **여섯 세션 동안 "미번역 0" 이었던 것이
완전하지 않았습니다.**

### ★ 아직 확인 안 된 것 (다음 세션이 이어서 할 일)

**1. 원거리(far field) 계열이 흐름 그래프에 없습니다.** ← 가장 큰 구멍

`q043_131_segregation`(격리)과 `q044_personalenclosure`(개인 밀폐)가 간선 그래프에
**없습니다.** 이 세션에서 사용자가 첫 캡처로 보낸 화면이 바로 격리 화면입니다.

원인: 간선 열쇠의 맥락 `ctx` 가 **제품 유형만** 담습니다.

```js
// crawl.js
const edgeKey = (ch) => ch.at + '@' + (ch.ctx || '') + '|' + ch.kind + ...
const ctxOf = (path) => (path && path.length ? String(path[0].v) : '');   // 첫 선택만
```

`q016_nearfieldsource`(근거리 발생원?)의 예/아니오가 **둘 다** `activityclass` 로 가므로
갈래가 갈린 것이 안 보이고, 갈림은 **한참 뒤 제어수단 단계**에서 드러납니다 —
근거리면 국소 제어, 원거리면 격리·개인 밀폐. `ctx` 가 그 답을 담지 않아
`q042_080_130_primarylocalizedcontrols@rbGranularMaterial` 이 **한 번만** 열거되고,
먼저 도착한 쪽(근거리)만 기록됐습니다.

**할 일:** `ctxOf` 를 제품 유형 + 근거리/원거리 답 (그리고
`q089_0_secondaryfarfieldsources` 의 예/아니오) 까지 담게 바꾸고 `crawl.js` 를 다시
돌립니다. 기존 간선은 `ctx` 형식이 달라지므로 백업 후 재수집이 필요합니다.

**번역은 영향 없을 가능성이 높습니다** — 두 화면의 `text`·`desc` 는 이미
`crawl_screens.json` 에 있고(각각 `desc` 5개) `gaps.json` 은 비어 있습니다.
빠진 것은 **그래프**입니다.

**2. 상호작용 탐침이 안 된 마법사 화면 4개**

```
q012_099_liquidweightfraction   (이번 세션에 새로 열린 화면)
q013_100_viscosity              (이번 세션에 새로 열린 화면)
q043_131_segregation
q044_personalenclosure
```

`node tools/probe_all.js` 는 `probe_done.json` 에 없는 것만 합니다.
찾을 것: 검증 오류 메시지, 마우스 올림 안내, 선택지별 `예시:`/`설명` 상자.

**3. postback 으로만 열리는 UI** — 링크를 따라가는 수집기(`crawl_pages.js`)는 `href`
로만 이동하므로 이 부류를 **구조적으로 못 봅니다.**

| 대상 | 단서 |
|---|---|
| 활동 이름 편집 | `a#aEditName01` (href 없음) |
| 활동 지속시간 편집 | `a#aEditDuration01` |
| 비노출 기간 편집 | `a#aEditNonExp` |
| 활동 2~4 슬롯 | 활동 목록 표에 `활동 구성...` 글자는 있는데 링크가 **숨겨져** 있습니다 (`ctl01` 만 보임). 여는 조건을 찾아야 합니다 — 지속시간을 넣어야 열릴 가능성. |

방법은 6차 세션 `probe_all.js` 와 같은 뒤집기입니다 — 누르기 전 글 스냅숏 → 누름 →
스냅숏 → 차이. 다만 **링크를 누르는** 판이 필요합니다 (아직 없습니다).

**4. 액체 시나리오의 결과·베이지안 화면** — 6차 세션은 **분말 이송**으로만 실행했습니다.
액체는 증기/미스트라 결과 화면의 단위·라벨이 다를 수 있습니다.
`node tools/run_model.js` 를 액체 구성으로 한 번 돌려 봐야 합니다.

**5. 공개 영역** — `register.aspx`(회원가입), 로그인 실패 메시지, 비밀번호 재설정·메일
흐름. 계정을 건드리는 일이라 손대지 않았습니다.

**6. 막힘 21개** — 검증 오류로 다음 화면에 못 간 조합입니다. 도착지를 모릅니다.
`tools/crawl_edges.json` 에서 `"dest": "BLOCKED"` 로 찾습니다.

### 새 도구

| 파일 | 하는 일 | 명령 |
|---|---|---|
| `labels.js` | (화면,맥락)마다 최단 경로로 되밟아 질문·선택지 **문구** 수집 | `node labels.js 200` |
| `flow.py` | 제품 유형별 흐름도(mermaid) + `flow.csv` + `flow.md` | `python flow.py` |
| `flow_one.py` | **한 장** 흐름도. 척추는 그대로, 분기 두 구간만 접음. `flow/one.json` 도 냄 | `python flow_one.py` |
| `flow_page.py` | 위 결과를 한 페이지 HTML 로 묶음 (게시용) | `python flow_page.py` |
| `lint_mmd.py` | 올리기 전 mermaid 문법 검사 | `python lint_mmd.py` |
| `probe_reset.js` | `활동 구성` 후 실제로 어디에 서는지 관찰 | `node probe_reset.js rbGas` |
| `probe_acts.js` | 활동 목록의 버튼·활동 슬롯 확인 | `node probe_acts.js` |
| `probe_fresh.js` | 취소→재적재로 활동 초기화 가능한지 시험 | `node probe_fresh.js` |
| `probe_why.js` | 잠긴 라디오의 이유 확인 | `node probe_why.js` |
| `probe_q.js` | 질문 문구가 DOM 어디 있는지 찾기 | `node probe_q.js` |
| `probe_cas.js` | CAS 번호로 물성이 채워지는지 확인 (결과: **안 채워짐**) | `node probe_cas.js 108-88-3` |

전부 `tools/` 안, 그 디렉터리에서 실행. 디버그 크롬이 9222 포트로 떠 있어야 합니다.

### 흐름 구조 (알아낸 것)

화면 63개 중 **9개가 공통 척추**입니다. 나머지는 두 군데서만 퍼지고 곧 합쳐집니다.

```
제품 유형 ──▶ [① 성상 질문] ──▶ 근거리 발생원? ──▶ 활동 등급
   ▲                                                    │
   │                                                    ▼
   │                                            [② 활동 세부 질문]
   │                                                    │
   └─ 활동 더 넣기 ◀─ 2차 원거리 ◀─ 확산 ◀─ 표면 오염 ◀─ 국소 제어(1차/2차)
                          │
                          └─▶ 활동 목록 (완료 및 실행)
```

- `1차 국소 제어` 는 `2차` 를 건너뛸 수 있습니다 (실측: 10개는 2차로, 4개는 바로 표면 오염)
- `2차 원거리 발생원` 에서 갈림 — 제품 유형으로 되돌아가면 활동 추가, 아니면 완료
- **이 그림에 격리·개인 밀폐가 없는 것이 위 「할 일 1」 입니다**

### 질문 문구가 있는 자리

| 화면 종류 | 위치 |
|---|---|
| 라디오 화면 | `<legend>` |
| 드롭다운 화면 | `legend` 가 **없음**. `#ctl00_cphMain_upMain` 안의 첫 글 덩이 |

### mermaid 함정 3개

1. **`<b>` 는 안 됩니다.** `<br/>` 만 됩니다.
2. HTML 에 넣을 때 **`<br/>` 를 `&lt;br/&gt;` 로 이스케이프**해야 합니다. 안 하면
   브라우저가 먼저 태그로 읽어 `textContent` 에 진짜 줄바꿈이 들어가고 노드 문법이
   깨집니다. (`flow_page.py` 의 `diagram()` 참조)
3. 원본에 `useMaxWidth: false` 를 넣지 않으면 mermaid 가 그림을 컨테이너 폭에 맞춰
   줄여서 글씨를 못 읽습니다. 그리고 확대할 때 **부모에 `zoom` 을 걸면 안 됩니다** —
   SVG 에 `width:100%` 가 붙어 있어 상쇄됩니다 (배율 숫자만 바뀌고 그림은 그대로).
   SVG 의 `width`/`height` 를 픽셀로 직접 지정해야 합니다.

### 결과물

- 흐름도 페이지: https://claude.ai/code/artifact/e789286a-f01e-4b20-8d60-9aaba309d39a
  (같은 파일 경로 `tools/flow/art-flow.html` 로 다시 게시하면 같은 주소가 갱신됩니다)
- 표(엑셀용): `tools/flow/flow.csv` — 435행
- 묶음 상자 속 내용: `tools/flow/one_detail.md`
- 그림 원본: `tools/flow/one.mmd`, `tools/flow/flow_*.mmd`

### 다시 만드는 순서

```
cd tools
node labels.js 200          # 문구가 빈 화면이 있을 때만
python flow.py
python flow_one.py
python lint_mmd.py          # 문법 검사
python flow_page.py         # -> flow/art-flow.html
# 그다음 Artifact 로 flow/art-flow.html 을 같은 주소에 게시
```

### 이어서 할 순서 (추천)

1. `ctxOf` 를 고쳐 원거리 계열을 그래프에 넣기 (「할 일 1」). 가장 값이 큽니다.
2. `node probe_all.js` 로 남은 4개 화면 탐침 (「할 일 2」).
3. postback 링크 누르는 판 만들기 (「할 일 3」). `probe_all.js` 를 본떠서.
4. 액체 구성으로 결과·베이지안 한 번 실행 (「할 일 4」).
5. 매번 끝에 `python tools/pipeline.py` — 빌드·테스트·미번역 검사까지 한 번에.

### 이 세션에서 사용자가 지적한 것 (그대로 지킬 것)

- "UI깨진다" — 게시 전에 CSS 계단 순서를 확인하십시오. `@media` 는 맨 끝.
- "글씨는 작은데 확대는 안먹네" — mermaid `useMaxWidth`, SVG 직접 크기 지정.
- "스크롤 없애줘" — 그림 틀에 `max-height` 를 주지 마십시오. 겹친 스크롤이 생깁니다.
- "상하 이동은 안되네" — 세로로 끌면 페이지를 움직이게 연결했습니다.

---

## 0-C. 6차 세션 (이어서) — 상호작용 자동 탐침, 사전 2,076개

*(번역 작업의 최신 기록입니다. 수집 데이터에 대해서는 위 `0-D` 절을 먼저 보십시오 — 이 절 시점의 간선 데이터에는 거짓 141개가 섞여 있었습니다.)*

### 숫자

| 항목 | 값 |
|---|---|
| 사전 | **2,076개** |
| 빌드 | `@version 1.6.0`, 277,606 bytes |
| 테스트 | **67/67 통과** |
| 남은 미번역 | **0개** (`gaps.json` 비어 있음) |
| 범위 밖 (이유 기록) | 43개 (`crawl_outofscope.json`) |

### 이번에 자동화로 찾은 것 (직접 확인으로는 못 찾는 부류)

| 무엇 | 개수 | 도구 |
|---|---|---|
| 선택지별 `예시:` 상자 | 78 | `sweep_boxes.js` |
| 검증 오류 메시지 | 25 | `probe_all.js` |
| 마우스 올려야 뜨는 안내 | 3 | `probe_all.js` (hover) |
| 측정자료 올리기 화면 + 도움말 3종 | 67 | `run_model.js` → `crawl_pages.js` |

### ★ 핵심 원리 — 이걸 놓치면 또 똑같이 빠집니다

지금까지의 수집기는 **'글이 있을 만한 곳'을 열거**해서 읽었습니다(텍스트 노드,
option, title, 설명 상자…). 그래서 **내가 모르는 곳은 영원히 못 봅니다.**

그래서 뒤집었습니다.

    조작 전 화면의 모든 글을 적는다  →  조작한다  →  다시 적는다
    차이 = 그 조작으로만 나타나는 글

`pagelib.js` 의 `textSnapshot()` 이 그 '모든 글' 이고, `probe_all.js` 가 조작을
돌립니다. **글이 어디서 나오는지 몰라도 잡힙니다.**

시도하는 조작: hover / 빈값 / 글자 / 큰값 / 음수 / 숫자와 범주 둘다 / 대화상자.

### 새 도구

| 파일 | 하는 일 |
|---|---|
| `probe_all.js` | 화면마다 6가지 조작 → 전후 글 차이 기록. 검증 메시지·툴팁·대화상자 |
| `sweep_boxes.js` | 선택지를 하나씩 눌러 `예시`/`설명` 상자 수집 |
| `run_model.js` | 활동을 끝까지 정상으로 채우고 **모델 실행**. 결과 화면이 열림 |
| `run_bayesian.js` | 분말 이송으로 재구성 → 모델 실행 → 베이지안 연쇄 실행 |
| `reach.js` | 저장된 경로 재생으로 특정 마법사 화면까지 이동 (`--examples`) |
| `scope_filter.py` | 번역 대상이 아닌 것 제외 + **이유 기록** |

`pagelib.js` 에 추가된 것: `textSnapshot`, `installDialogSpy`/`readDialogSpy`,
`hoverAll`, `stuffBadValues`, `optionBoxes`, `loadScenario`.

**`installDialogSpy` 는 `confirm` 에 항상 `false` 를 돌려줍니다.** 삭제 확인이
진행되지 않게 하는 안전장치입니다. 절대 `true` 로 바꾸지 마십시오.

### 도구 자체의 결함 두 개도 잡았습니다

1. **`verify.js` 가 헛 검증을 하고 있었습니다.** 브라우저에 스크립트가 이미
   돌면 파일 빌드를 안 넣고 넘어가서 **옛 빌드를 검증**했습니다.
   → 이제 `Page.addScriptToEvaluateOnNewDocument` 로 **문서 로드 전에** 파일
   빌드를 심습니다. 중복 방지 가드 덕분에 Tampermonkey 쪽이 스스로 물러납니다.
2. **보이지 않는 요소의 속성까지 긁었습니다.** `<style title="ART Standard">` 가
   미번역으로 잡혔습니다. `textSnapshot` 에 가시성 검사를 넣었습니다.

### 번역하지 않기로 판단한 것 (이유 있음)

* **CSV 열 이름** — `dataset`, `concentration`, `isnondetect`,
  `uncertaintyfactor`, `ratio`, `exposure`, `site`, `worker`.
  사용자가 파일에 그대로 적어야 하는 식별자입니다. **번역하면 업로드가 깨집니다.**
  설명문만 번역했습니다.
* 학술 인용문헌(`science.aspx`), 기관 약어, 쿠키 이름, 단위, `PDF`/`Excel`,
  IIS/ASP.NET 진단 화면. 전부 `crawl_outofscope.json` 에 이유와 함께 있습니다.

### 남은 일 (정직하게)

1. **베이지안 업데이트 실행 결과 화면** — `run_bayesian.js` 로 시도 중입니다.
   노출 라이브러리에 유사 데이터가 있는 조합이어야 진행됩니다. 2차 세션 기록:
   **분말 이송 → 분말 낙하**로 구성하면 11개가 나옵니다. 액체 분무는 데이터 없음.
   막히면 로그(`run_bayesian.log`)의 '보이는 버튼' 목록을 보고 클릭 문구를 고치십시오.
2. **가지치기로 건너뛴 갈래 131개** — 한 컨트롤의 선택지 3개가 같은 화면으로 가면
   나머지는 안 걸어봤습니다. 문구는 수집했지만 그 아래 화면이 같다는 것은 가정입니다.
   `crawl.js` 의 `CONVERGE_AFTER` 를 아주 크게 두고(가지치기 사실상 해제)
   `crawl_edges.json` 의 `dest === 'PRUNED'` 를 `null` 로 되돌린 뒤 다시 돌리면 됩니다.
3. **활동 2~4개짜리 시나리오, 비노출 기간 화면** — 활동 하나만 구성해 봤습니다.
4. **비밀번호 재설정처럼 이메일이 오가는 흐름** — 계정 조작이 필요해 안 건드렸습니다.

### 커버리지를 주장할 때

**"`gaps.json` 이 비었다" 는 "수집한 것 중엔 안 빠진 게 없다" 는 뜻입니다.**
수집 자체가 빠진 요소는 잡히지 않습니다. 그러니 커버리지를 말할 때는
**"어떤 요소를, 어떤 조작 뒤에 읽었는가"** 를 적으십시오. 화면 수만 세면
이번처럼 통째로 놓칩니다.

---

## 0-B. 6차 세션 — `예시:` 상자가 통째로 빠져 있었습니다

**이 절이 가장 최신입니다.** 위 `0-A`(5차)는 "번역 끝" 이라고 적었지만 **틀렸습니다.**

### 사용자가 잡은 것

`q018_056_104_fracturingabrasionsolids.aspx` 화면 캡처. 오른쪽 `예시:` 상자가 영어:

    예시:
    • Milling operations
    • Lathe
    • Circular saw

### 원인 — 두 상자는 **선택할 때만** 채워집니다

| 상자 | 어디서 오는가 | 4차 순회기가 본 화면 |
|---|---|---|
| `설명` | `Descriptions` 전역 **또는** 포스트백 | 전역 있으면 전역에서 읽음 / 없으면 라디오 눌러 읽음 |
| `예시:` | **전역 없음. 포스트백뿐** | **전역이 있는 화면에서는 한 번도 안 봤음** |

4차 순회기 `crawl.js` 의 `sweep()` 은 `hasTa && !h.desc` 일 때만 라디오를 하나씩
눌렀습니다. `Descriptions` 전역이 있는 화면은 "설명은 전역에서 다 읽었으니 클릭
불필요" 로 판단해 넘어갔고, **그 화면의 예시 상자는 영원히 빈 상태로 수집**됐습니다.

증거: `crawl_screens.json` 의 그 화면 항목이 `title` 과 `attr` 두 개뿐이었습니다.

### 예시 상자의 실제 DOM — 제목이 밖에 있습니다

    <span>예시:</span>
    <div id="divExamplesList">
      <ul><li>Milling operations</li><li>Lathe</li><li>Circular saw</li></ul>
    </div>

`예시:` 머리글이 `divExamplesList` **밖에** 있습니다. 그래서 "머리글이 예시인
목록을 찾아라" 는 방식은 실패합니다. **`#divExamplesList` id 로 직접 잡으십시오.**
`pagelib.js` 의 `optionBoxes()` 가 그렇게 합니다.

### 새 도구

| 파일 | 하는 일 |
|---|---|
| `tools/pagelib.js` → `optionBoxes()` | `#divExamplesList` 의 li 들 + 설명 상자를 읽음 |
| `tools/pagelib.js` → `loadScenario(name)` | 시나리오 목록에서 이름으로 불러오기. **'삭제' 링크는 건너뜀** |
| `tools/reach.js` | 저장된 경로를 재생해 특정 마법사 화면까지 이동. `--examples` 로 선택지별 덤프 |
| `tools/sweep_boxes.js` | 마법사 61개 화면 전부에서 선택지별 예시/설명 수집 |

```bash
node reach.js fracturingabrasionsolids --examples   # 한 화면만 확인
node sweep_boxes.js 200                             # 전 화면 수집 (재개 가능)
python scope_filter.py && python gaps.py --by-page  # 구멍 확인
```

`sweep_boxes.js` 는 예시를 다 모은 화면에 `screens[url].example` 을 남겨
다음 실행에서 건너뜁니다(빈 배열도 '확인했음' 표시입니다).

### 새 브라우저 프로필로 시작할 때 막히는 지점

마법사 화면은 **시나리오가 불러와져 있어야** 열립니다. 아니면 `myscenarios.aspx`
로 튕깁니다. `reach.js` / `sweep_boxes.js` 는 목록에서 `ART_SCENARIO`
(기본 `claude-translanguage`)를 찾아 **불러오기**를 먼저 누릅니다.

디버그 크롬 띄우기:

```powershell
Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList @(
  '--remote-debugging-port=9222',
  '--user-data-dir=C:\Users\user\.claude-parallel\art-korean\.chrome-debug',
  '--no-first-run',
  'https://www.advancedreachtool.com/loggedin/myscenarios.aspx')
```

이 프로필은 ART 로그인 + 유저스크립트가 이미 들어 있습니다.
**사용자가 쓰는 크롬 창은 절대 건드리지 마십시오** (5차 세션의 SendKeys 사고 참조).

### 교훈 — "번역 끝" 이라고 쓰지 마십시오

`gaps.json` 이 비어 있다는 것은 **"수집한 것 중에는 안 빠진 게 없다"** 는 뜻일
뿐입니다. **수집 자체가 빠진 화면 요소**는 잡히지 않습니다. 커버리지를 주장하려면
"어떤 요소를 어떤 조건에서 읽었는가" 를 적어야 합니다. 이번에 빠진 것은
'선택 후에만 나타나는 요소' 였고, 같은 부류가 또 있을 수 있습니다:

- 마우스를 올려야 뜨는 것 (`title` 은 덮었지만 CSS 툴팁은 미확인)
- 잘못 입력해야 뜨는 검증 메시지 (일부만 덮음)
- 모델 실행 중에만 보이는 진행 화면
- 인쇄/내보내기 미리보기

---

## 0-A. 5차 세션 (2026-08-22 저녁) — 설치 가이드 제작

*주의: 이 절은 "번역 종료" 라고 적었지만 **틀렸습니다.** 위 `0-B` 절을 보십시오 —
선택할 때만 나타나는 `예시:` 상자가 통째로 빠져 있었습니다.*

### 숫자로 본 현재

| 항목 | 값 |
|---|---|
| 사전 (`art-ko-dict.json`) | **1,893개** |
| 빌드 (`art-korean.user.js`) | **331,066 bytes**, `@version 1.4.1` |
| 테스트 (`tools/test.js`) | **67/67 통과** |
| 순회한 화면 | 75개 · 간선 482개 · **미탐색 0** |
| 남은 미번역 | **0개** (`tools/gaps.json` 비어 있음) |
| 설치 가이드 | `ART-한글판-설치가이드.html` (1.0 MB, 단독 실행) |

### 이번 세션에 한 일

1. **번역 완료 확인.** 세션 시작 때 `tools/crawl_todo.json` 의 135개를
   "미탐색 135개" 로 잘못 읽었습니다(4차 문서에는 그런 표현이 없습니다 —
   0-0 절은 처음부터 "미탐색 0" 으로 적혀 있습니다).
   `crawl_todo.json` 은 **순회가 수집한 영어 문자열 모음**입니다. 키가 문자열,
   값은 **빈 문자열**(번역칸 자리)입니다. 미탐색 간선은 `crawl_edges.json` 에서
   `dest === null` 인 것을 세야 하고, 지금 482개 전부 탐색됐습니다.
   **번역이 남았는지 보려면 `gaps.json` 을 보십시오**(수집분 − 사전).
   → `crawl_todo.json` 은 이름이 오해를 부릅니다. 개수를 "남은 일" 로 읽지 마십시오.

2. **개발자 모드 요구사항 발견 — 이전 문서가 틀렸습니다.** (아래 별도 절)

3. **설치 가이드 제작.** 실제 화면 캡처 12장 + 스크립트 전문 내장.
   - 단독 파일: `ART-한글판-설치가이드.html`
   - 아티팩트: <https://claude.ai/code/artifact/94d2b16d-6d57-4ad1-94b6-51fb015e5b0c>
   - 재빌드: `cd tools/shot && python build-guide.py`

4. **안전한 화면 캡처 도구** `tools/shot/` (아래 별도 절)

---

### ★ 정정 — 개발자 모드도 반드시 켜야 합니다

**이전 README/HANDOVER 는 "개발자 모드는 지금은 무관"이라고 적었습니다. 틀렸습니다.**

크롬 v151.0.7922.172 에서 **사용자 스크립트 허용은 켜 둔 채 개발자 모드만** 껐다 켜며
확인했습니다.

| 개발자 모드 | 사용자 스크립트 허용 | ART 화면 |
|---|---|---|
| 켜짐 | 켜짐 | **한국어** |
| **꺼짐** | 켜짐 | **영어** |
| 다시 켬 | 켜짐 | **한국어** |

**함정:** 개발자 모드를 꺼도 `chrome://extensions` 세부정보의
"사용자 스크립트 허용" 항목은 **화면에 그대로 남아 있고 스위치도 파란색 그대로**입니다.
켜져 있는 것처럼 보이는데 실제로는 동작하지 않고, 경고도 없습니다.

→ 설치 안내는 **항상 두 스위치를 함께** 말해야 합니다.

#### 부수 발견 — `file://` 로 설치하려면 세 번째 스위치가 필요합니다

`.user.js` 파일을 크롬 창에 끌어다 놓아 설치하려면
**"파일 URL에 대한 액세스 허용"**(같은 세부정보 화면, 기본 꺼짐)도 켜야 합니다.
같은 파일로 이 스위치만 껐다 켜며 확인했습니다 — 껐을 때 무반응(설치 화면 자체가 안 뜸),
켰을 때 정상. 지금 가이드는 이 경로를 쓰지 않지만(붙여넣기 한 방법만 안내),
필요해지면 이 사실을 기억하십시오.

---

### ★★ 화면 자동화 사고 — 절대 반복하지 마십시오

**`SendKeys` 로 키보드 입력을 보내지 마십시오.**

이번 세션에서 크롬 창을 앞으로 가져와 주소를 타이핑하려고
`SetForegroundWindow` + `SendKeys` 를 썼습니다. 윈도우는 백그라운드 프로세스의
포그라운드 탈취를 막기 때문에 **창 전환이 조용히 실패했고, 키 입력이 그때 포커스를
갖고 있던 다른 앱(VS Code·카카오톡)으로 들어갔습니다.** 사용자에게 사과하고
카카오톡 대화방을 확인해 달라고 요청해야 했습니다.

또 하나의 실수: `Get-Process chrome | Where MainWindowTitle -ne ""` 로 창을 골랐더니
**사용자의 실제 크롬**이 먼저 잡혔습니다. 디버그용 크롬을 쓰려면 **PID 를 명시**하십시오.

#### 대신 이렇게 하십시오 (`tools/shot/`)

| 파일 | 하는 일 |
|---|---|
| `cdpnav.js` | CDP 로만 조작. 키보드·포커스 안 건드림 |
| `winshot.ps1` | `PrintWindow` 로 창 내용만 캡처. 창을 앞으로 안 끌어옴 |

```bash
cd tools/shot
node cdpnav.js tabs                      # 탭 목록
node cdpnav.js nav "chrome://extensions/" # 이동 (chrome:// 도 됨)
node cdpnav.js scroll 900                 # 마우스 휠 스크롤
node cdpnav.js eval "document.title"      # JS 실행
node cdpnav.js shot out.png               # CDP 캡처 (chrome:// 은 불가)
node cdpnav.js altk                       # Alt+K 를 그 탭에만 전송
ART_TAB=1 node cdpnav.js eval "..."       # 탭 인덱스 직접 지정
ART_PORT=9333 node cdpnav.js ...          # 다른 디버그 크롬
```

```powershell
# chrome:// 페이지는 CDP 캡처가 막혀 있어 이걸 씁니다
.\winshot.ps1 -ProcId <크롬PID> -Out shot.png -Width 1560 -Height 1200
```

#### 캡처할 때 걸린 함정 네 가지

1. **DPI.** 이 PC 는 150% 배율입니다. DPI 를 인식하지 않으면 `GetWindowRect` 는
   가상 좌표(1707×1067)를 주는데 `PrintWindow` 는 물리 픽셀(2560×1600)로 그려서
   **왼쪽 위 2/3 만 잘려 나옵니다.** `winshot.ps1` 은
   `SetThreadDpiAwarenessContext(-4)` 로 해결했습니다.
2. **창이 가려지면 빈 화면이 나옵니다.** 크롬이 백그라운드 창 렌더링을 멈춥니다.
   일반 https 페이지는 `cdpnav.js shot`(CDP 캡처)을 쓰면 창이 안 보여도 잡힙니다.
3. **CDP `nav` 는 탭을 바꾸지 않습니다.** 이동시킨 탭과 화면에 보이는 탭이 다를 수
   있습니다. `cdpnav.js` 는 `/json/activate` 를 먼저 호출해 해결했습니다.
4. **인포바.** 유저스크립트 설치 페이지를 다녀오면
   "이 웹사이트에서 앱·확장 프로그램을 추가할 수 없습니다" 줄이 남아 레이아웃이 밀립니다.
   `/json/new` 로 **새 탭을 만들고 기존 탭을 닫으면** 깨끗해집니다.

#### 스샷을 다시 찍어야 할 때

`tools/shot/screenshots/` 에 최종본 12장이 있습니다. 다시 찍으려면
디버그 크롬(포트 9222, 프로필 `.chrome-debug`)을 띄우고 위 도구로 화면을 만든 뒤
`winshot.ps1` 로 잡아 WebP 로 줄이십시오(품질 88, 폭 1280 기준 30~60 KB).

- **"설치" 화면을 찍으려면** 이미 설치돼 있어서 "재설치"로 뜹니다.
  `@namespace` 만 바꾼 사본을 임시로 서빙/열어서 찍고 **설치 버튼은 누르지 마십시오.**
- **개인정보 확인.** ART 로그인 상태에서는 우측 상단에 사용자 이름이 찍힙니다.
  창 폭을 1500 으로 잡으면 잘려 나갑니다. 이미 찍었다면 PIL 로 흐리게 처리하십시오.

---

### 설치 가이드 구조

`tools/shot/install-tpl.html` 이 본문 템플릿입니다.
**`ART-한글판-설치가이드.html` 을 직접 고치지 마십시오** — `build-guide.py` 가 덮어씁니다.

가이드는 **5단계**입니다.

1. 크롬 웹스토어에서 Tampermonkey 설치
2. **개발자 모드 켜기**
3. Tampermonkey 세부정보 열기
4. **사용자 스크립트 허용 켜기**
5. 스크립트 붙여넣기 (페이지 안의 **전체 복사** 버튼 → 대시보드 → ＋ → `Ctrl+A` → `Ctrl+V` → `Ctrl+S`)

#### 설치 방법이 하나뿐인 이유

처음에는 "로컬 서버로 설치"를 대안으로 넣었다가 뺐습니다. **성립하지 않는 방법이었습니다** —
`python -m http.server` 를 하려면 `.user.js` 파일이 이미 손에 있어야 하는데,
가이드를 받은 사람에게 없는 게 바로 그 파일입니다. 파이썬도 필요하고요.
그다음 "파일로 저장해서 끌어다 놓기"를 넣었다가 사용자 판단으로 그것도 뺐습니다.
**지금은 붙여넣기 한 방법뿐이고, 그게 맞습니다.**

#### 스크립트 전문을 페이지에 심는 이유

아티팩트 뷰어는 **페이지가 시작하는 다운로드를 차단합니다.** `<a download>` 도
data:/blob: 도 동작하지 않습니다. 그래서 파일을 링크로 줄 수 없고,
`ART_SRC` 문자열로 심어 **클립보드 복사**로 전달합니다.
복사 버튼은 3단계로 물러납니다 — `navigator.clipboard` → `execCommand('copy')` →
텍스트 칸을 펼쳐 전체 선택(`Ctrl+C` 안내).

`json.dumps` 후 **`<` 를 `<` 로 바꿔야** 소스 안의 문자열이 `</script>` 로
오인되지 않습니다. `build-guide.py` 가 처리합니다.

---

### 남은 일

**번역 자체는 끝났습니다** — `gaps.json` 비어 있음, 테스트 67/67, 가이드 완성.
다만 아래 세 건은 아직 열려 있습니다. "없음" 이 아닙니다.

**(1) 사용자가 준 논문을 아직 읽지 않았습니다.**

    tools/source/art_mech_paper.pdf   (235 KB)
    tools/source/art_mech_paper.txt   (pdftotext 추출본, 1203행)

Fransman et al., *ART: Development of the mechanistic model*. 활동 등급/하위등급
전체 표와 보정인자(MF) 배수표가 있어, 오래 "미확보" 로 남아 있던 Marquart 2011
계열 표를 상당 부분 대체합니다. 용어 대조와 누락 등급 확인에 쓰십시오.
`pdftotext` 에 **`-layout` 을 쓰지 마십시오**(5절 함정).

**(2) 결과·베이지안 화면을 현재 빌드로 다시 확인하지 않았습니다.**

번역은 이미 있습니다(`results` 51개 + `bayesian` 57개, 2~3차 세션에 수집).
문제는 4차 세션 순회기가 시나리오를 미완성으로 남겨 그 화면들이 렌더링되지
않았고(`System.InvalidOperationException: Found invalid enabled activity`),
그래서 **현재 빌드로 눈으로 확인한 기록이 없다**는 점입니다.
활동 하나를 끝까지 정상으로 채우고(지속시간 480분) 모델을 실행한 뒤
`node verify.js /loggedin/mechanisticresults.aspx` 로 확인하십시오.
도달 경로는 아래 "결과·베이지안 화면 — 도달 및 수집 완료" 절에 그대로 있습니다.

**(3) 그 밖에:**

- **`support.aspx` 자동 검증 타임아웃** — `node verify.js /support.aspx` 가
  `ART_CALL_TIMEOUT=90s` 로도 실패합니다. 화면 자체는 번역돼 있고 눈으로 확인했습니다.
  문서 목록이 길어 트리워커가 오래 걸리는 것으로 추정합니다. **사용에 지장 없습니다.**
- **오류 메시지 문구** — 일부러 잘못 입력해야 뜨는 것들이라 순회로 다 못 봅니다.
  실사용 중 영어가 보이면 `Alt+C` → `Alt+E` 로 모아 사전에 추가하십시오.
- **컨소시엄에 공식 로케일 제안** — 7절 참조. 승인되면 설치 자체가 불필요해집니다.

### 사용자와 이야기할 때

- **쉬운 말로, 짧게.** 결론 먼저. 내부 용어·절 번호·백분율 표를 설명 없이 쓰지 마십시오.
- "그래서?" 같은 짧은 되물음은 **답이 길고 어려웠다는 신호**입니다.
  더 자세히 말하지 말고 더 짧게 다시 말하십시오.
- **노가다는 넘기지 마십시오.** 수집·순회·대조는 직접 하십시오.
  사용자에게 `Alt+C` 누르며 돌아다니라고 시키지 마십시오.
- 사용자가 지적하면 **추측으로 답하지 말고 실제로 실험해서** 확인하십시오.
  이번 세션의 개발자 모드 건이 그렇게 잡혔습니다.

---

## 0-0. 4차 세션 종료 시점 상태 *(숫자가 낡았습니다 — 위 0-A 절을 먼저 보십시오)*

### 숫자로 본 현재

| 항목 | 값 |
|---|---|
| 사전 (`art-ko-dict.json`) | **1,887개** (빈 값 0, 구역 간 충돌 0) |
| 빌드 (`art-korean.user.js`) | **330,864 bytes**, `@version 1.4.0` |
| 테스트 (`tools/test.js`) | **67/67 통과** |
| 순회한 화면 | **75개** (마법사 63 + 마법사 밖 13, 이전 세션은 47) |
| 순회 간선 | 482개, **미탐색 0** (전부 걸어봤음) |
| 남은 미번역 | **0개** |
| 범위 밖으로 제외 | 38개 (`tools/crawl_outofscope.json`, 이유 함께 기록) |

### 이번 세션에 한 일 (요약)

1. **구조 결함 4건** — 사전으로는 절대 못 고치는 것들
   - `<textarea readonly>` 설명 상자 (0-1절)
   - 브라우저 탭 제목
   - `confirm()` / `prompt()` 대화상자
   - **중복 실행 시 페이지 멈춤** — 관찰자 두 개가 서로의 변경을 물어 무한 루프.
     `window.__ART_KO_ACTIVE__` 가드 추가. 테스트 `[14]` 로 덮음.
2. **순회기 전면 재작성** — 기존 도구가 못 돌던 이유 6가지를 찾아 고침 (0-1절 3~4항)
3. **이전 세션이 한 번도 못 본 화면 19개 도달** — 진공 이송, 연마 블라스팅 기법·방향,
   분말 분무 방향, 압축/파쇄 밀폐수준, 액체 표면 분무·공간 분무·정지/교반 액면,
   도포, 고속 공정, 하부 적재, 낙하 액체, 이송 적재 방식, 격리(발생원),
   개인 밀폐, 2차 국소 제어
4. **번역 13라운드** (`tools/tr_round1.py` ~ `tr_round13.py`)

### 지금 당장 해야 할 일 (우선순위 순)

**(1) 사용자에게 최신 빌드를 설치하게 하기 — 아직 안 됐을 가능성이 큼**

사용자는 세션 중간에 "아직 미번역인 것도 있어" 라고 했는데, 확인해 보니 주요
상태 문구는 이미 사전에 있었습니다. **브라우저의 Tampermonkey 가 옛 빌드(1,567개)
일 가능성이 큽니다.** 먼저 이것을 확인하십시오.

    # 클립보드에 올리기 (PowerShell)
    $p = "C:\Users\user\.claude-parallel\art-korean\art-korean.user.js"
    $t = Get-Content -Raw -Encoding UTF8 $p
    Set-Clipboard -Value $t
    (Get-Clipboard -Raw).Length   # 파일 길이와 같은지 확인

붙여넣기: Tampermonkey 대시보드 → `ART 한국어` → `Ctrl+A` → `Ctrl+V` → `Ctrl+S`
→ ART 페이지 새로 고침. **버전이 1.4.0 으로 바뀌었는지 목록에서 확인**하게 하십시오.
(설치 함정은 2차 세션 기록 참고: '사용자 스크립트 허용' 스위치)

**(2) 사용자가 준 논문 활용 — 아직 안 읽었습니다**

세션 끝에 사용자가 직접 가져온 자료입니다.

    tools/source/art_mech_paper.pdf   (235 KB)
    tools/source/art_mech_paper.txt   (pdftotext 추출본, 1203행)

Fransman et al., *Advanced REACH Tool (ART): Development of the mechanistic model*.
**활동 등급/하위등급 전체 표와 보정인자(MF) 배수표**가 실려 있습니다. 인수인계
문서가 오래 '미확보' 로 적어 둔 Marquart 2011 계열 표의 상당 부분이 여기 있습니다.

할 일:
- 용어 대조 — `_meta.glossary` 와 어긋나는 표현이 없는지
- 활동 등급/하위등급 이름을 사전과 대조해 빠진 것 찾기
- **`pdftotext` 에 `-layout` 을 쓰지 마십시오.** 표 열이 뒤섞입니다(5절 함정)

**(3) 결과 · 베이지안 화면 — 이번 순회에서만 비었습니다 (번역은 이미 있음)**

*정정:* 처음에 "가장 큰 남은 구멍" 이라고 적었는데 과장이었습니다. 사전에는
`results` 51개 + `bayesian` 57개가 이미 들어 있습니다(2~3차 세션 수집, 아래
"결과·베이지안 화면 — 도달 및 수집 완료" 절 참조). 빠진 것은 **번역이 아니라
현재 빌드로의 재확인**입니다.

`mechanisticresults.aspx` 는 **제목만** 수집됐고 `bayesianresults.aspx` 는
ASP.NET 예외를 냈습니다. 원인은 순회기가 시나리오를 **미완성 상태로 남겼기**
때문입니다 (`System.InvalidOperationException: Found invalid enabled activity`).

해야 할 일: 활동 하나를 **끝까지 정상으로** 채우고(지속시간 480분 포함) 모델을
실행한 뒤, 그 상태에서 결과/베이지안/측정자료 화면을 훑으십시오.
`tools/crawl_pages.js` 를 그 상태에서 다시 돌리면 됩니다.

참고로 결과 화면 도움말 문서(`pagehelp-mechanisticresults.aspx`)는 이미 번역
했습니다 — 전 교대시간 vs 장기 평균, 백분위수, 신뢰구간 설명. 화면 본체가 남았습니다.

**(4) `support.aspx` 검증 타임아웃**

`node verify.js /support.aspx` 가 `타임아웃 Runtime.evaluate` 로 실패합니다.
`ART_CALL_TIMEOUT` 을 이미 90초로 올렸는데도 안 됩니다. 이 화면만 따로 봐야 합니다
(문서 목록이 길어 `harvest()` 의 트리워커가 오래 걸리는 것으로 의심).

**(5) `credits.aspx` 남은 영어 18개 — 판단 필요, 결함 아님**

    The / ART / HSE / AFSSET / CEFIC / LRI / BOHS / GSK / TNO / and / , the / , Shell, Eurometaux,

기관 **약어**와 로고 링크 사이에 끼인 **연결어 조각**입니다. 기관의 정식 명칭은
로고의 `title` 속성에 한국어로 넣어 두었습니다(마우스를 올리면 보입니다).
연결어는 링크 사이 조각이라 한국어 어순으로 맞출 수 없습니다. **그대로 두는 것이
낫다고 판단했습니다.** 뒤집으려면 링크를 감싸는 문장 전체를 다시 쓰는 수밖에 없고,
그건 DOM 구조를 바꾸는 일이라 이 프로젝트의 원칙(표시 계층만 건드림)에 어긋납니다.

**(6) 사용자가 말한 '상태창'을 아직 특정하지 못했습니다**

사용자 표현: "상태창이라던가.. 아직 미번역인 것도 있어".
확인한 것 — `Communicating with server. Please wait...`, `Please wait...`,
`Loading...`, `Note!`, `ART — Progress` 는 **모두 사전에 있습니다.**
그래서 (1)의 옛 빌드 문제일 가능성이 가장 큽니다. (1)을 먼저 처리한 뒤에도
남아 있으면 화면 캡처를 받으십시오.

### 실행 순서 요약

    cd C:\Users\user\.claude-parallel\art-korean\tools

    # 번역을 넣은 뒤 항상
    python pipeline.py            # 변형 -> 빌드 -> 테스트 -> 점검 -> 남은 구멍

    # 순회 (디버그 크롬 9222 필요)
    node crawl.js 500             # 마법사 전체 갈래 (재개 가능)
    node crawl_pages.js 120       # 마법사 밖 화면
    python scope_filter.py        # 범위 밖 문구 제외
    python gaps.py --by-page      # 남은 구멍 확인

    # 실제 페이지 검증
    node verify.js /loggedin/myscenarios.aspx /credits.aspx

### 디버그 크롬 주의

세션 중 사용자가 같은 브라우저를 쓰고 있어서 ART 탭이 사라졌습니다.
**사용자가 보고 있는 탭을 가로채지 마십시오.** 새 탭을 여십시오.

    fetch('http://127.0.0.1:9222/json/new?' + encodeURIComponent(url), {method:'PUT'})

`cdp.js` 의 `connect()` 는 `advancedreachtool` 이 들어간 첫 페이지 탭을 잡습니다.

---

## 0. 지금 상태 (2026-08-22 세션, 이어받는 사람이 먼저 볼 것)

### 추가 세션 (4차) — 구조 결함 2개 + 전체 갈래 순회기

사용자가 `claude-translanguage` 시나리오를 만들어 주고 "모든 경우의 수를 적용해서
모든 페이지를 확인하고 번역하자"고 했습니다.

#### 1) 설명 상자가 화면마다 두 종류였습니다 (가장 중요)

스크린샷의 `q043_131_segregation` 설명 상자가 영어로 남아 있던 이유입니다.

| 화면 | 요소 | 3차까지 |
|---|---|---|
| `q004`, `q042` 등 | `<div id="divDescription">` | 번역됨 |
| `q043`, `q045` 등 | `<textarea readonly name="ctl00$cphMain$txtDescription">` | **번역 안 됨** |

`textarea` 는 사용자가 직접 쓰는 칸(메모 `taUserNotes`)을 망치지 않으려고
`SKIP_TAGS` 에 넣어 두었습니다. 그래서 사전에 아무리 넣어도 이 상자는 안 바뀝니다.

고친 방법 — `doDescTextareas()` 추가.
* **`readonly` 또는 `disabled` 인 textarea 만** 번역합니다. 사용자 입력칸은 그대로.
* 이 상자에는 `name` 이 있어 **폼 전송에 포함**됩니다. 버튼과 똑같이 전송 직전에
  원문으로 되돌립니다 → `restoreAllButtons()` 선택자를
  `input[data-art-en], textarea[data-art-en]` 로 넓혔습니다.
* 테스트 `[12]` 6개 추가 (**58/58 통과**).

**알려진 한계(문서화됨, 고치지 않음):** textarea 의 `.value` 를 스크립트로 바꾸면
MutationObserver 가 울리지 않습니다(자식 텍스트 노드가 그대로라서). 그래서 복원
직후에는 다음 포스트백까지 영어로 남습니다. 실제 사이트는 설명을 채울 때마다
포스트백이 요소를 새로 심으므로 문제가 안 됩니다. **타이머로 다시 번역하지
마십시오** — 복원과 전송 사이에 끼어들면 서버가 한국어를 받습니다.

#### 2) 브라우저 탭 제목

`Configure Activity - ART Mechanistic Model` 이 그대로였습니다. `doTitle()` 추가.
서버로 가지 않는 순수 표시용 문자열이라 복원이 필요 없습니다.

#### 3) 전체 갈래 순회기 `crawl.js` — 기존 도구들이 왜 못 돌았는가

세 가지를 찾았습니다. **다시 만들 때 이걸 모르면 똑같이 막힙니다.**

1. **ART 는 "값 직접 입력" 과 "범주 선택" 중 하나만 받습니다.** 둘 다 채우면
   오류 메시지도 없이 화면이 안 넘어갑니다. `q004 발진성`, `q007 질량분율`,
   `q009 공정온도` 에서 기존 도구가 전부 여기서 막혀 있었습니다.
   → `next()` 는 **아무것도 더하지 않고 먼저 눌러 보고**, 막힐 때만 빈 칸을 채우고,
   그래도 막히면 숫자 칸을 비우고 다시 누릅니다. 순서가 중요합니다.
2. **1차 국소 제어를 고르면 그 아래에 하위 라디오 그룹이 새로 뜹니다.**
   숫자/드롭다운만 채우면 `q042` 에서 계속 막힙니다. → `next()` 2단계에서
   체크 안 된 라디오 그룹을 먼저 채웁니다.
3. **클릭 직후 URL 을 읽으면 아직 이전 화면입니다.** 고정 sleep 이나 URL 단순
   비교로는 포스트백 시작 직전을 '안정' 으로 오인합니다. → `cdp.js` 의 `act()` 가
   `window.__artCrawlMark` 표식이 사라졌는지(문서 교체), PageRequestManager 가
   busy 인지(부분 포스트백)를 보고 기다립니다. 변화가 시작되지 않으면 즉시 반환.

#### 4) 순회 대기열은 **반드시 디스크에 저장**해야 합니다

처음에는 대기열을 메모리에만 두었습니다. 그러니 다시 돌릴 때
"목적지를 이미 알고 그 화면도 훑었으니 건너뛴다" 규칙에 전부 걸려서 **1경로 만에
끝났습니다.** 아직 안 가본 자식 갈래가 대기열과 함께 사라진 것입니다.

지금 구조: `crawl_edges.json` 에 **(화면, 선택) 간선**을 하나하나 적어 둡니다.

    dest === null       아직 안 걸어봄
    dest === '/x.aspx'  걸어본 결과
    dest === 'BLOCKED'  화면이 안 넘어감
    dest === 'PRUNED'   수렴 가지치기로 생략

간선마다 평생 딱 한 번만 걷고, 언제 끊어도 이어집니다.

속도 장치(없으면 몇 시간 → 며칠):
* **이어 걷기** — 깊이 우선이라 다음 간선은 대개 '한 걸음 더'. 재생 생략.
* **`이전` 버튼 되감기** — 형제 갈래는 경로 길이가 같아 '이어 걷기'가 안 먹습니다.
  공통 접두사까지 `이전` 을 눌러 되돌립니다.
* **수렴 가지치기** — 한 컨트롤의 선택지가 3개 연속 같은 화면으로 가면 나머지
  형제는 안 걸어봅니다(질량분율 구간 등). 선택지 문구는 `sweep()` 이 이미 읽었고
  잃는 건 '그 아래 화면'인데 어차피 같습니다. **건너뛴 개수를 로그에 남깁니다.**

#### 4-1) 순회기가 놓치던 세 가지 (전부 수정)

**(a) 활동 등급 목록은 제품 유형마다 다릅니다.** 그런데 순회기가 화면을 URL
하나로만 취급해서, 처음 도착한 제품 유형의 등급 목록만 열거하고 끝났습니다.
그래서 액체 전용 등급(`q026` 표면 분무, `q028` 정지 액면, `q029` 도포,
`q030` 고속 공정, `q032` 하부 적재)에 영원히 못 갔습니다.
→ 간선 키와 '열거 완료' 표시에 **문맥(ctx = 경로 첫 선택 = 제품 유형)** 을 넣습니다.

    edgeKey = at + '@' + ctx + '|' + kind + '|' + name + '|' + value

화면 텍스트 훑기(무거운 sweep)는 URL 당 한 번이면 충분하지만, **선택지 문구는
문맥마다 다르므로** 방문할 때마다 가벼운 `harvest()` 로 다시 긁습니다(포스트백 0회).

의도한 한계: 문맥을 경로 **첫** 선택으로만 봅니다. 더 깊은 분기가 등급 목록을
바꾸는 경우는 못 덮습니다. 제품 유형이 주 분기축이라 이 정도로 잡았습니다.

**(b) 활동 등급 화면은 라디오(등급) + 드롭다운(하위등급) 조합입니다.** 라디오만
갈래로 잡으면 하위등급별 하위 질문 화면에 못 갑니다.
→ 등급을 하나씩 골라 그때 뜨는 하위등급 목록을 읽고 **쌍(pair) 갈래**를 만듭니다.
`applyChoice` 가 라디오 먼저(포스트백으로 목록 갱신), 그다음 드롭다운을 고릅니다.
구분자는 `PAIR_SEP = '~~'` — 눈에 보이는 문자를 쓰십시오. 보이지 않는 문자를 쓰면
`split('')` 이 문자 단위로 쪼개지는 사고가 납니다(실제로 겪음).

**(c) 마법사는 활동을 끝내면 제품 유형 화면으로 되돌아갑니다.** 그 지점에서 자식
갈래를 또 등록해 같은 트리를 두 번 탔습니다(증상: `q003` 간선이 8개가 아니라 16개,
깊이 13~16 간선이 잔뜩 생김 = 얻는 것 없이 비용 2배).
→ **순환 차단**: 경로에 이미 나온 화면에 다시 도착하면 자식 갈래를 등록하지 않습니다.

#### 5) 새 파일

| 파일 | 역할 |
|---|---|
| `tools/cdp.js` | 지속 연결 CDP + `act()` 포스트백 대기. `drive.js` 는 명령마다 프로세스를 새로 열어 순회에 부적합 |
| `tools/pagelib.js` | 페이지에서 돌릴 함수 모음. **실제 함수로 정의해 `fn.toString()` 으로 보냅니다** — 문자열/템플릿 리터럴로 감싸면 정규식이 깨집니다(함정 2) |
| `tools/crawl.js` | 마법사 전체 갈래 깊이 우선 순회 (간선 목록 방식) |
| `tools/crawl_pages.js` | 마법사 밖 화면(내 시나리오/결과/베이지안/지원)을 링크 따라 순회. **지우기·로그아웃 링크는 절대 안 누름** |
| `tools/gaps.py` | 수집분 − 사전 = 진짜 구멍 |
| `tools/apply_gaps.py` | 접두사로 지목 → 전체 문장은 `gaps.json` 에서 가져와 사전에 넣음. 0개/2개 이상 일치면 실패시킴 |
| `tools/pipeline.py` | 변형 -> 빌드 -> 테스트 -> 점검 -> 남은 구멍. 번역 넣은 뒤 항상 이걸 돌립니다 |
| `tools/nearfield_variants.py` | 같은 질문의 근거리장 판 생성 |
| `tools/titlecase_variants.py` | 제목대문자 변형 (비상용, 파이프라인 밖) |
| `tools/tr_round1.py` ~ `tr_round7.py` | 4차 세션 번역 정의 (라운드별) |
| `tools/probe.js` | 한 갈래만 걸으며 설명 상자 정체 확인 |

#### 5-1) 번역을 넣은 뒤에는 항상 `python pipeline.py`

순서가 중요합니다. 변형 생성이 먼저여야 원본에서 파생됩니다.

    철자 변형 -> 활자 변형 -> 근거리장 짝 -> 빌드 -> 테스트 -> 사전 점검 -> 남은 구멍

테스트가 실패하면 **배포하지 마십시오.** 합격 기준은 폼 전송 데이터 동일성입니다.

| 변형 도구 | 무엇을 맞추는가 |
|---|---|
| `spelling_variants.py` | minimise/minimize, far-field/far field, vapour/vapor |
| `typography_variants.py` | `m²`/`m2`, 붙임표(–/-), `< 0.1`/`<0.1` |
| `nearfield_variants.py` | 같은 질문의 **근거리장 판** (사전에 원거리장 판만 있던 계열) |
| `titlecase_variants.py` | 제목대문자 — **비상용, 파이프라인에 없음** (아래 참고) |

`titlecase_variants.py` 는 파이프라인에 넣지 않습니다. ART 는 선택지를 문장형,
화면 제목을 제목대문자로 씁니다 (`Fracturing of powders...` /
`Fracturing of Powders...`). 규칙으로 만들면 677개가 나오는데 대부분 화면에 없고
`0.5 Kg`, `E.g. Copper` 같은 오변환이 생깁니다. 활동 등급 이름(157개)으로 좁혀
한 번 넣어 두었고, 그 뒤로는 순회기가 실제 제목을 수집하게 둡니다.
단위·`e.g.` 보호 규칙은 도구 안에 들어 있으니 비상시엔 그대로 쓰면 됩니다.

#### 5-2) 번역 넣는 방법 — 긴 문장을 손으로 다시 적지 마십시오

`apply_gaps.py` 를 씁니다. **앞부분(접두사)** 으로만 지목하고 전체 문장은
`gaps.json` 에서 가져옵니다. 오타 한 글자로 매칭이 조용히 깨지는 것을 막습니다.
접두사가 0개 또는 2개 이상과 맞으면 **실패시킵니다** (조용히 넘어가면 번역이
사라진 줄도 모릅니다).

```python
# tools/tr_roundN.py
import apply_gaps
apply_gaps.run([
    ('Sources are partially segregated from the work environment by isolating the '
     'source in a separate room (e.g. with open doors', 'descriptions',
     '발생원을 별도의 실(...)에 두어 ...'),
])
```

    python tr_roundN.py --check   # 매칭만 확인
    python tr_roundN.py           # 사전에 넣기

**번역문에 유니코드 이스케이프를 쓰지 마십시오.** 한글을 그대로 적습니다.
이스케이프로 적으면 나중에 문구를 고칠 때 검색이 안 됩니다 (이 세션에서
'별로'를 '비교적'으로 고치려다 겪었습니다).

#### 5-3) 진단용으로 모으지만 아직 번역하지 않는 것

`crawl_otherattrs.json` — `aria-label`, `summary`, `data-*` 속성에 들어 있는 영어.
유저스크립트의 `ATTRS` 는 `title`, `placeholder`, `alt` 만 봅니다. 이 파일에
뭔가 쌓이면 그 속성을 `ATTRS` 에 추가할지 판단하십시오. 표시용 속성이라
번역해도 서버로 가는 데이터는 바뀌지 않습니다. **번역 단위가 아니므로
`crawl_todo.json` 에는 넣지 않습니다** (`aria-label=...` 형태라 사전 키로 쓸 수
없습니다).

#### 6) `gaps.py` 가 하는 세 가지 보정 (이거 없으면 다 헛일)

1. **`Descriptions` 전역 값은 번역 여부와 무관하게 항상 영어입니다.** 페이지 JS
   데이터이지 DOM 이 아닙니다. "남은 영어" 로 세면 안 되고 **사전과 직접 비교**해야
   합니다. 이걸 착각해 "번역이 안 됐다" 고 오판했습니다.
2. **설명 값에 HTML 과 엔티티가 섞여 있습니다** (`<br/>`, `<ul><li>`, `m&#179;`).
   `innerHTML` 로 들어가므로 DOM 에서는 조각마다 텍스트 노드가 따로 생기고
   엔티티는 풀립니다. **사전 키도 조각 단위 + 엔티티 해제 상태**여야 맞습니다.
   통째로 넣으면 절대 안 맞습니다.
3. **다른 문장의 앞부분인 것은 버립니다.** 초기 `pagelib` 이 설명을 80자로 잘라
   기록했습니다. 잘린 문장을 사전에 넣으면 부분 치환(14자 이상)에 걸려 화면에
   '한국어 + 영어 꼬리' 가 나옵니다.

#### 7) 그 밖의 함정

* **마법사 화면은 직접 `nav` 로 못 들어갑니다.** 순서를 강제해 `q002_7` 로
  튕겨냅니다. `grab_descriptions.js` 가 이래서 절반만 걷었습니다.
* **히어독(`<<'EOF'`)이 백슬래시를 한 번 먹습니다.** 파이썬으로 JS 를 패치할 때
  `'
'` 을 검색 문자열에 쓰면 실제 개행이 되어 안 맞습니다. `NL = chr(92) + 'n'`
  로 만드십시오. 이 세션에서도 두 번 걸렸습니다.
* 200줄 넘는 JS 를 히어독으로 쓰면 셸 파싱이 깨집니다. Write 도구를 쓰십시오.

---

### 추가 세션 (3차) — 숨어 있던 세 종류의 구멍을 메웠습니다

사용자가 실제로 쓰면서 "구멍이 많다"고 지적했습니다. 화면 캡처를 보고 원인을 셋으로
갈랐고 전부 처리했습니다. **사전 1221 → 1567개, 테스트 50/50 유지.**

#### 1) `설명` 상자 — 전역 `Descriptions` 에 다 들어 있습니다

국소 제어 화면 등의 `설명` 상자(`DIV#divDescription`)는 선택지를 고를 때 내용이
채워집니다. **아무것도 선택하지 않은 상태에서는 비어 있어서 수집기가 통째로
놓쳤습니다.** 9,700자가 그냥 빠져 있었습니다.

핵심: 내용은 페이지 스크립트의 전역 `Descriptions` 객체에 **전부** 들어 있습니다.
선택지를 하나하나 클릭할 필요 없이 전역만 읽으면 됩니다 — `grab_descriptions.js`.

```bash
node grab_descriptions.js        # 전 화면 순회 -> descriptions.json
```

**중요: 통째로 키를 만들면 안 됩니다.** 이 값들은 `innerHTML` 로 주입되고 안에
`<br/>`, `<ul><li>`, `<em>` 이 들어 있어서 DOM 에서는 **여러 텍스트 노드로 나뉩니다.**
유저스크립트는 텍스트 노드 단위로 치환하므로 태그로 쪼갠 **조각**을 키로 써야 합니다.
쪼개면 중복도 크게 줄어듭니다(39개 원문 → 88개 조각이지만, 겹치는 앞머리가 많아
실제 신규는 72개). `descriptions` 섹션에 72개 추가했습니다.

#### 2) `?` 도움말 팝업 — qTip, 클릭해야 존재합니다

`?` 아이콘은 qTip(jQuery) 이고, 누르면 서버에서 조각을 받아 `.qtip-content` 에
넣습니다. DOM 삽입이므로 MutationObserver 가 잡아 번역은 되지만, **클릭하지 않으면
DOM 에 없어서 수집이 불가능합니다.** `grab_help.js` 가 화면마다 눌러서 긁습니다.

팝업이 있는 화면은 2개뿐이었고 조각 6개(번역 대상 4개)입니다. 텍스트가 γ, χi 같은
기호 요소에서 잘려 **세 토막**으로 나뉘므로 토막별로 키를 만들었습니다.
`help` 섹션.

> **ART 자체 버그:** 도움말 조각이 raw .aspx 로 서빙돼서 팝업 맨 앞에
> `<%@ Page Language="C#" %>` 와 CSS `@import` 가 그대로 보입니다. 영어 원본에서도
> 똑같이 보입니다. 번역 대상이 아니므로 `grab_help.js` 가 걸러냅니다. 고치려면
> 표시 자체를 숨겨야 하는데, 번역 범위를 넘으므로 손대지 않았습니다.

#### 3) 화면 제목 — 텍스트 노드가 갈립니다

`원거리장 1차 배출 발생원 — 2차 국소 제어` 처럼 제목이 두 조각으로 렌더링돼서
뒷조각이 자주 빠집니다. `grab_titles.js` 로 긁어 제목 10개를 추가했습니다.

#### 검증

새 빌드를 실제 화면에 주입(`node drive.js inject`)해 확인했습니다. q042 / q042_5 /
q015 / q028 에서 남은 영어는 **언어 전환기(`English`/`Deutsche`/…), 사용자 시나리오
이름(`xylene`/`toluene`), 도구명 `XLUNIFAC`** 뿐입니다 — 전부 의도적 원문 유지입니다.

#### 활동 등급 순회 (2차 세션에서 이어짐)

`walk_classes.js` 로 액체 계열 7개 등급의 하위 질문 화면 6개를 새로 확보했습니다.
`sweep_activities.js` 는 선택만 바꾸고 `다음` 을 누르지 않아 그 화면들에 도달하지
못했습니다. 위저드 화면 번역률 89.7% → 94.0%.

분말·고체·금속 계열은 아직 못 갔습니다. `step_to_class.js` 가 물성 질문 통과를
자동화하지만 `q009_096_processtemperature.aspx` 에서 막힙니다 — 화면이
`Temperature out of range...` 를 띄웁니다. 액체 경로의 허용 범위를 넘는 값(1500)을
넣었기 때문이고, **범위 안의 값을 넣으면 넘어갈 것으로 보입니다.**

#### 표기 변형 규칙 세 개 추가

| 사이트 | 추출본 | 처리 |
|---|---|---|
| `0.3 m²` `30 m³` | `0.3 m2` `30 m3` | 상단첨자 규칙 (`typography_variants.py`) |
| `far-field` | `far field` | 복합어 하이픈 + 숫자 범위 간격 변형 |
| `< 0.1 m²` | `<0.1 m2` | 비교기호 뒤 공백 변형 |

`m²` 하나가 13개 항목을 막고 있었습니다. **새 화면에서 영어가 남으면 번역을 새로
쓰기 전에 먼저 표기 변형인지 의심하십시오.**

#### 다시 밟지 말 것

- **페이지로 보내는 코드에 정규식을 쓰지 마십시오.** 이번에도 걸렸습니다.
  `grab_titles.js` 가 `/\s+/g` 때문에 모든 `s` 를 지워서
  `Unsupported solid material` 이 `Un upported olid material` 로 나왔습니다.
  정규화는 Node 쪽에서 하십시오 (인수인계서 '함정 2').
- **드롭다운이 이미 한국어입니다.** 자리표시자를 영어(`no activity`, `choose`)로
  걸러내면 `(하위 활동 등급 없음)` 을 실제 항목으로 골라버리고, 그 포스트백이
  선택을 무효화해 `다음` 이 먹지 않습니다. **인덱스로 고르십시오.**
- **숫자칸과 범주 드롭다운을 같이 채우면 안 됩니다.** ART 는 둘 중 하나만 받고,
  둘 다 채우면 오류도 없이 화면이 안 넘어갑니다.
- **클릭 직후 `dump()` 는 예전 URL 을 줍니다.** URL 이 바뀔 때까지 폴링하십시오.
  `drive.js eval` 은 기다리지 않으므로 대기용으로 쓰면 헛돕니다 — `Atomics.wait`.

#### 새 도구

| 파일 | 용도 |
|---|---|
| `walk_classes.js` | 활동 등급마다 `다음` 을 눌러 하위 질문 화면까지 진입 |
| `step_to_class.js` | 물성 질문을 인덱스 선택으로 자동 통과 |
| `grab_descriptions.js` | 전역 `Descriptions` 에서 설명 상자 내용 일괄 추출 |
| `grab_help.js` | `?` qTip 팝업 클릭 후 내용 추출 |
| `grab_titles.js` | 화면 제목 조각 추출 |


### 추가 세션 (같은 날, 이어서) — 활동 등급 하위 질문 화면 확보

**앞 세션이 남긴 가장 큰 구멍(options 226개 / questions 41개 미확인)의 정체가
확인되고 상당 부분 메워졌습니다.**

정체는 이것이었습니다: `sweep_activities.js` 는 활동 등급/하위등급의 **선택만**
바꿨을 뿐, `다음` 을 눌러 그 등급의 **하위 질문 화면**(교반 정도, 낙하 높이,
분무 방향, splash/submerged 등)에는 들어가지 않았습니다. 그래서 그 화면들의
문자열이 통째로 미확인으로 남아 있었습니다.

새 도구 `walk_classes.js` 가 등급마다 `다음` 을 눌러 하위 질문 화면까지 들어갑니다.
액체 계열 7개 등급 전부 순회해 6개 화면을 새로 확보했습니다:

```
q028_066_114_liquidopensurfaceundisturbed.aspx
q028_5_066_5_114_5_handlingcontaminatedobjects.aspx
q029_067_115_spreadingliquidproducts.aspx
q030_068_116_liquidshighspeedprocesses.aspx
q032_070_118_bottomloading.aspx          <- splash/submerged
q026_5 / q026_7 (분무 방향·기법)
```

| | 전 | 후 |
|---|---|---|
| 사전 항목 | 1221 | **1396** |
| 본 화면 | 43 | **49** |
| 위저드 화면 번역률 | 89.7% | **94.0%** |
| 테스트 | 50/50 | 50/50 |

남은 미번역 35개는 전부 (a) 이 세션이 만든 임시 시나리오 이름(`ZZ-*`,
`temp activity`, `dust handling`), (b) 순수 단위값(`0.3 ACH`, `480 min`,
`1 - 10 l/minute`), (c) 이미 한국어인 문자열의 오탐입니다.
**위저드 화면은 사실상 다 덮였습니다.**

#### 표기 차이 규칙을 세 개 추가했습니다

문서 기반 사전이 실제 화면과 어긋나는 방식이 세 가지 더 발견됐습니다. 전부
자동 변형으로 처리했으므로, 앞으로 번역을 추가한 뒤에도 그냥 다시 돌리면 됩니다.

| 사이트 | 추출본 | 처리 |
|---|---|---|
| `0.3 m²` `30 m³` | `0.3 m2` `30 m3` | `typography_variants.py` 에 상단첨자 규칙 추가 (+27) |
| `far-field` | `far field` | 복합어 하이픈 변형 (+89, 숫자 범위 간격 포함) |
| `< 0.1 m²` | `<0.1 m2` | 비교기호 뒤 공백 변형 (+44) |

`m²` 하나가 13개 항목을 막고 있었습니다. **새 화면을 만나면 번역을 새로 쓰기 전에
먼저 표기 변형인지 의심하십시오.**

#### 아직 막힌 것 — 분말·고체·금속 계열 등급

액체 외 제품 유형(분말/과립, 고체 물체, 액중 분말, 페이스트, 고온 금속)의 활동
등급 하위 질문 화면은 아직 못 갔습니다. 등급 목록이 **세션의 제품 유형**에 따라
갈리는데, 제품 유형을 바꾸려면 q003 에서 라디오를 고르고 `다음` 으로 확정한 뒤
중간 물성 질문들(발진성·수분·질량분율·온도)을 통과해야 합니다.

`step_to_class.js` 를 새로 만들어 그 통과를 자동화했지만
`q009_096_processtemperature.aspx` 에서 막혔습니다. 원인은 확인했습니다 —
화면이 `Temperature out of range. Please enter a temperature in °C in the range shown.`
을 띄웁니다. 액체 경로의 허용 범위(최대 150도)를 넘는 값(1500)을 넣었기 때문입니다.
**범위 안의 값을 넣으면 넘어갈 것으로 보입니다.** 다음 세션이 여기서 이어가면 됩니다.

그 과정에서 확인한 세 가지 (다시 밟지 않도록):

1. **드롭다운이 이미 한국어입니다.** `autowalk.js` 와 초기 `walk_classes.js` 가
   자리표시자 `— 선택 —` / `(하위 활동 등급 없음)` 을 실제 항목으로 착각해
   멈췄습니다. 영어 기준 필터(`no activity`, `choose`)로는 안 걸러집니다.
   **텍스트로 고르지 말고 인덱스로 고르십시오**(`step_to_class.js` 가 그렇게 합니다).
2. **숫자칸과 범주 드롭다운을 같이 채우면 안 됩니다.** ART 는 둘 중 하나만 받고,
   둘 다 채우면 오류도 없이 화면이 안 넘어갑니다. 숫자를 우선하십시오.
3. **클릭 직후 `dump()` 는 예전 URL 을 줍니다.** 포스트백 경합입니다. URL 이 바뀔
   때까지 폴링해야 합니다. 또 `drive.js eval` 은 기다리지 않으므로 대기용으로
   쓰면 헛돕니다 — `Atomics.wait` 로 실제 블로킹 대기를 하십시오.

#### 계정 정리 — 할 일 없음

이 세션도 `Save Changes` 를 누르지 않았습니다. 임시로 만든 활동 이름
(`temp activity` 등)은 세션에만 있고 계정에는 저장되지 않았습니다.


### 이어받으면 바로 이것부터 (2026-08-22 마지막 상태)

**사용자가 설치해서 실제로 쓰기 시작했습니다. 그리고 "번역에 구멍이 많다"고 했습니다.**

구멍을 메우는 게 다음 할 일입니다. 그런데 **사용자에게 `Alt+C` → 돌아다니기 →
`Alt+E` 를 시키지 마십시오.** 그건 노가다이고, 사용자가 명시적으로 "그 작업을 너한테
시키고 싶었다"고 했습니다. **디버그 크롬 + CDP 로 직접 순회해서 수집하십시오.**
이 세션에서 그 방식으로 43개 화면을 자동으로 돌았습니다. 방법은 아래 "수집 환경
다시 붙이기"에 다 있습니다.

아직 안 가본 화면이 어디인지는 이렇게 확인합니다:

```bash
cd C:\Users\user\.claude-parallel\art-korean\tools
cut -f1 collected.by-page.tsv | sort -u      # 이미 간 화면 43개
```

사전 1221개 중 **723개만 실제 화면에서 확인**됐습니다(59%). 나머지 498개는 4장 PDF
에서 온 것이라 실제 화면과 글자가 다를 수 있습니다. 섹션별 확인율:

| 확인 많이 됨 | 확인 적게 됨 |
|---|---|
| `info` `bayesian` `scenarios` `site` (89~94%) | `alerts` (0%) |
| `wizard` `results` `examples` (72~84%) | `workflow` `nav` `auth` (19~35%) |
| `guidance` (59%) | `options` `questions` (40%) |

`options`(386개 중 226개)와 `questions`(68개 중 41개)가 가장 큰 미확인 덩어리입니다.
활동 등급별 하위 질문 화면을 더 돌면 여기가 메워집니다. `alerts` 0% 는 오류·경고라
일부러 잘못된 입력을 넣어야 나옵니다.

### 설치 — 반드시 알아야 할 함정 하나

사용자가 설치할 때 여기서 30분 넘게 막혔습니다.

**크롬 확장 설정에서 "사용자 스크립트 허용" 을 켜야 합니다.**
`chrome://extensions` → Tampermonkey → "세부정보" → 아래로 스크롤 → 스위치.

이걸 안 켜면 **Tampermonkey 설치됨 + 스크립트 설치됨 + 토글 초록색인데 화면은 영어**
입니다. 오류도 안 납니다. 최신 크롬(MV3)에서 확장별 스위치로 바뀐 부분입니다.

증상이 같아서 헛짚기 쉬운 것들 — 이번에 전부 원인이 **아니었습니다**:

- ~~`chrome://extensions` 의 개발자 모드 (예전 크롬 요구사항, 지금은 무관)~~
  **정정 (2026-08-22, 크롬 v151):** 개발자 모드도 **반드시 켜야 합니다.** 꺼져 있으면
  "사용자 스크립트 허용"이 켜진 것처럼 보여도 동작하지 않습니다. 대조 실험으로 확인.
- `@match` 에 `www` 누락 (실제로 고치긴 했음 — `*://*.advancedreachtool.com/*`)
- 크롬 프로필 불일치
- Tampermonkey 저장소를 디스크에서 읽어 확인하는 것 → **읽지 마십시오.**
  `Local Extension Settings` 가 비어 있어도 스크립트는 설치돼 있습니다.
  Tampermonkey 는 다른 곳에 저장합니다. 이것 때문에 잘못된 진단을 두 번 했습니다.

**진단은 Tampermonkey 아이콘 메뉴로 하십시오.** 문제가 있으면 맨 위에 파란 줄로
`사용자 스크립트 허용 확장 설정을 활성화하세요` 라고 직접 알려줍니다.

편집기에 붙여넣고 `Ctrl+S` 하는 설치 방법은 조용히 실패할 수 있습니다. 대신:

```bash
cd C:\Users\user\.claude-parallel\art-korean
python -m http.server 8899 --bind 127.0.0.1
```

크롬에서 `http://localhost:8899/art-korean.user.js` 를 열면 설치 화면이 뜹니다.

### 사용자와 이야기할 때

**쉬운 말로 짧게.** 이 세션에서 "말하는 난이도 좀 낮추자, 너무 이해하기 어렵게
말한다"는 피드백을 받았습니다. 백분율 표, 섹션 이름(`options`, `guidance`), 절 번호
(`3.2절`) 같은 것을 설명 없이 쓰지 마십시오. 결론 → 쓸 수 있는지 → 사용자가 할 일,
이 순서로 짧게. "그래서?" 같은 되물음은 답이 길고 어려웠다는 신호입니다.

---

### 계정 정리 — 할 일 없음

수집용으로 `ZZ-DELETE-ME-i18n` 임시 시나리오를 만들었지만 **`Save Changes` 를 누른 적이
없어 계정에 저장되지 않았습니다.** 시나리오 목록에는 사용자의 기존 2개
(`xylene / 20260225 PDC연습`, `toluene / 20260225 PDC 연습`)만 있고 그대로입니다.
지울 것도, 되돌릴 것도 없습니다.

> 다음 세션이 마법사를 또 몰아야 한다면 같은 방식을 쓰십시오 — **새 시나리오를 만들되
> 저장하지 않는다.** 세션에만 존재하므로 계정이 깨끗하게 유지됩니다.

### 이번 세션에 달라진 것

이전까지 사전은 **4장 PDF에서 추출**한 것이었고 실제 화면과 대조된 적이 없었습니다.
이번에 로그인된 크롬에 CDP 로 붙어 실제 DOM 을 수집하는 경로를 뚫었고, 문서 기반
사전이 실제 화면에서 얼마나 안 맞는지가 드러났습니다.

| | 이전 | 이후 |
|---|---|---|
| 사전 행 수 | 555 | **1223** (고유 키 1222) |
| 로그인 전 화면 덮임률 | 48.8% | **번역 대상 100%** |
| 평가 도구 화면 UI 덮임률 | 42.7% | **97.2%** (607개 / 40개 화면, 남은 17개는 전부 임시 시나리오 이름·단위값) |
| 수집한 실제 DOM 문자열 | 0 | **936개 / 40개 화면** |

평가 도구 화면에서 남은 7개는 전부 의도적 원문 유지입니다 — 임시 시나리오 이름,
도구명 `XLUNIFAC`, 그리고 `0.3 ACH`·`1 ACH` 같은 순수 단위값(4절 "숫자와 단위는 원문 유지").

신설 섹션: `scenarios`(28) `nav`(19) `wizard`(148). `guidance` 는 37 → 140,
`options` 는 254 → 366 으로 늘었습니다.

### 문서 기반 사전이 실제로 실패한 네 가지 방식

이게 이번 세션의 핵심 교훈입니다. 앞으로 남은 화면도 같은 식으로 깨질 것이므로
**새 화면을 만나면 반드시 실제 DOM 을 수집해 대조하십시오. 문서만 보고 채우면 안 됩니다.**

1. **철자 계열 (영/미)** — 4장은 `pelletized`, 사이트는 `pelletised`. `-ize/-ise`,
   `-yze/-yse`, `vapor/vapour`, `fiber/fibre`. → `spelling_variants.py`
2. **활자 계열** — `pdftotext` 가 도(°) 기호를 문자 `o` 로, 엔대시(–)를 하이픈(-)으로
   뭉갭니다. 사전 `Room temperature (15 - 25 oC)` vs 화면 `Room temperature (15 – 25 ºC)`.
   → `typography_variants.py`
3. **문단 분할** — PDF 는 한 문단으로 합쳐 놓았지만 실제 페이지는 나눠서 표시합니다.
   785자짜리 사전 키가 화면에서는 407자 + 160자로 쪼개져 둘 다 매칭 실패.
   → 화면 조각을 각각 키로 넣고 번역도 그에 맞게 나눠야 합니다.
4. **문서에 아예 없는 문자열** — `Next`, `Previous`, `Cancel`, `OK`, 페이지 제목,
   툴팁(`title` 속성), 검증 메시지. 가장 자주 보이는 것들인데 4장에는 없습니다.

세 번째만 손으로 해야 하고, 1·2번은 스크립트가 자동으로 변형 키를 만듭니다.
**번역을 추가한 뒤에는 항상 두 스크립트를 다시 돌리십시오:**

```bash
python spelling_variants.py && python typography_variants.py && python build.py && node test.js
```

### 실제 사이트 검증 완료 (2026-08-22) — 합격

유저스크립트를 실제 로그인 페이지에 주입해 **네트워크로 나가는 POST 본문**을 직접
확인했습니다. Tampermonkey 없이 CDP `Runtime.evaluate` 로 주입하면 `@grant none` 과
같은 조건이라 프로토타입 패치까지 그대로 동작합니다 (`node drive.js inject`).

`Input.dispatchMouseEvent` 로 **진짜 마우스 이벤트**를 보내 확인한 결과:

| 화면 표시 | 실제 전송값 |
|---|---|
| 버튼 `다음` | `ctl00$...$btnNext = "Next"` |
| 드롭다운 `실온 초과 (25 - 50 ℃)` | `ddlTemperature = "2"` |
| 하위등급 `공간 내 액체 분무` | `hfActivityClass = "12"` |
| 라디오 (한국어 라벨) | `ActivityClass = "rbLiquidSprayApplication"` |

POST 본문에 한국어가 하나도 섞이지 않았습니다. 게다가 `__ASYNCPOST=true` 였으므로
3.2절 **경로 4**(`PageRequestManager`)가 실제로 작동한 것이 확인됐습니다.

> **`element.click()` 으로 검사하지 마십시오.** `pointerdown` 이 발생하지 않아
> 3.2절 경로 1a 를 건너뜁니다. 반드시 `Input.dispatchMouseEvent` 를 쓰십시오.
> 또 `new FormData(form, button)` 을 그냥 호출하면 버튼 캡션이 한국어로 보입니다 —
> 설계상 **전송 직전에** 복원하므로 정상입니다(`test.js:100`, `:130` 참조).
> 오탐이니 놀라지 마십시오. 판단은 네트워크 본문으로 하십시오.

6절의 미검증 위험(페이지 JS 가 option 텍스트로 분기)도 확인했습니다. 한국어로 표시된
활동 등급을 골랐을 때 서버가 정확한 하위등급을 돌려주었습니다. **텍스트 기반 분기는
발견되지 않았습니다.**

### 라이브 검증에서 잡은 실제 결함 — `pick()` (수정 완료)

`doOptions` / `doButtons` / `doAttrs` 가 모두 `root.querySelectorAll(...)` 만 썼습니다.
`querySelectorAll` 은 **자손만** 찾는데, MutationObserver 는 추가된 노드 **자신**을
넘겨줍니다. ASP.NET UpdatePanel 이 `<option>` 을 통째로 갈아끼우자 그 노드가 번역에서
통째로 빠졌습니다 — 실제 화면에서 하위등급 드롭다운이 영어로 남았습니다.

`pick(root, sel)` 헬퍼를 넣어 root 자신도 포함하도록 고쳤고, 실제 사이트에서 재확인했습니다.

기존 테스트 `[6]` 이 이걸 못 잡은 이유가 중요합니다 — 컨테이너 `<div>` 를 통째로
추가해서 자손 탐색으로 걸렸기 때문입니다. **노드를 직접 추가하는** 회귀 테스트를
`[11]` 로 추가했습니다 (테스트 45 → 50개).

### 결과·베이지안 화면 — 도달 및 수집 완료

`Input.insertText` 로 뚫렸습니다. 막혔던 원인은 **활동 이름·지속시간 인라인 편집기**였고,
ASP.NET 인라인 편집기는 `.value` 설정 + `change` 이벤트(합성 이벤트)를 무시합니다.
CDP `Input` 도메인으로 **진짜 키 입력과 마우스 이벤트**를 보내야 반응합니다.

`drive.js` 에 두 명령을 추가했습니다:

```bash
node drive.js mclick "a[title='Edit name of activity']"   # 진짜 마우스 클릭
node drive.js type "textarea.field" "temp activity"       # 진짜 키 입력
node drive.js mclick ".inplace-edit .save-button"         # 저장
```

편집기의 입력칸은 **이름은 `textarea.field`, 지속시간은 `input.field`** 로 서로 다릅니다.
그리고 `input.field` 를 그냥 찾으면 숨겨진 것이 잡히니, 반드시 보이는
`.inplace-edit` 안에서 찾으십시오.

도달한 화면과 덮임률:

| 화면 | 결과 |
|---|---|
| `mechanisticresults.aspx` (노출 예측 결과) | 수집 완료 |
| `aboutanalogousdata.aspx` (베이지안 진입) | 수집 완료 |
| `browseedb.aspx` (노출 데이터 라이브러리) | 수집 완료 |
| `progress.aspx` (모델 실행 중) | 수집 완료 |
| 위 59개 문자열 | **번역 대상 100%** |

신설 섹션 `results`, `bayesian`.

### 베이지안 업데이트 — 도달 및 수집 완료

`bayesianresults.aspx` 까지 갔습니다. 처음에 막혔던 이유는 시나리오가 노출
라이브러리와 안 맞아서였습니다("No suitable scenarios were found"). **분말 이송
(Transfer of powders → Falling of powders)** 으로 구성하니 라이브러리가 11개
시나리오를 돌려주었습니다. 액체 분무는 데이터가 없습니다.

경로:

```
결과 → Proceed to Bayesian Model → aboutanalogousdata
     → Select from the ART exposure data library → browseedb
     → 시나리오 이름 클릭(선택)  ← div.exp-col-section-left label 을 mclick
     → #ctl00_cphRightPane3_lbBayesian (Proceed to Bayesian update)
     → prebayesian → Run Bayesian Model → bayesianresults
```

**모든 화면이 도달·수집·번역 완료입니다.** 신설 섹션 `results`, `bayesian`.

### 제품 유형 분기 순회 — 완료

| 제품 유형 | 상태 |
|---|---|
| Liquids | 수집 완료 |
| Powders, granules or pelletised material | 수집 완료 |
| Solid objects | 수집 완료 |
| Paste, slurry or wet powder | 수집 완료 |
| Powders dissolved in a liquid | 수집 완료 |
| Gas / Fibrous material / Hot or molten metal | **불필요 — 아래 참조** |

**가스·섬유상·고온 금속은 순회할 필요가 없습니다.** ART 홈페이지가
"for lack of suitable calibration data, ART can not (for the time being) be used for
the assessment of fumes, fibres, gases, and dust resulting from emissions during hot
metallurgical processes" 라고 명시합니다. 실제로 재료 선택지에도
`Glass (this is outside the applicability domain of the current ART)` 형태로
적용 범위 밖임이 표시되며, 그 문자열들은 이미 번역돼 있습니다. 설계상 막다른 길입니다.

> **분기를 바꿀 때 함정:** 같은 활동에서 제품 유형만 바꾸면 이전 답변이 남아 있어
> 하위 질문이 갱신되지 않습니다(가스를 골라도 고체 경로를 그대로 지나갑니다).
> **분기마다 새 시나리오를 만드십시오.** 활동 슬롯 2~4 를 쓰려면 먼저 그 활동에
> 이름과 지속시간을 넣어야 `Configure activity...` 버튼이 나타납니다.

### 정보성 페이지 — 완료

`science` / `support` / `training` / `credits` / `cookie` 5개 페이지를 수집(137개)해
**UI 와 산문만** 번역했습니다(신설 섹션 `info`, 47개). 덮임률 12.4% → 47.4%.

남은 72개는 전부 의도적 원문 유지입니다:

- 학술 인용 — 저자 목록 10개, 논문 제목 10개, 저널명(`Ann Occup Hyg`, `J. Environ. Monit.`)
- 기관 공식명칭 — `Health and Safety Executive`, `Bundesanstalt für Arbeitsschutz…` 등
- 쿠키 기술명 — `ART.ASPXAUTH`, `ASP.NET_SessionId`, `'eu_compliance'`, `art_cookie`
- 약어·제품명·이메일 — `TNO`, `IOM`, `CEFIC`, `Advanced REACH Tool`, `ART@hse.gov.uk`

**인용을 번역하면 원문을 찾을 수 없게 되므로 오히려 해롭습니다.** 기관명도
마찬가지로 원어가 검색 가능한 형태입니다. 이 결정은 사용자가 내린 것입니다.

번역한 것 중에는 링크로 문장이 쪼개진 조각들이 있습니다(`in the UK.`,
`) offer a variety of`, `project is jointly funded by`). 쿠키 고지와 같은
분할 패턴이므로 앞뒤가 이어지도록 어순을 맞춰 두었습니다.

> `The`, `and`, `, the` 같은 **맨 연결어는 일부러 번역하지 않았습니다.** 2자~3자
> 짜리 일반 단어를 정확 일치 키로 넣으면 사이트 어디서든 치환되어 엉뚱한 곳에서
> 튀어나옵니다. 표시 계층이라 위험하지는 않지만 화면이 지저분해집니다.

### 범위 결정 — 노출 데이터베이스 내용은 원문 유지

`browseedb.aspx` 에서 수집된 문자열 중 98개가 **노출 데이터베이스의 데이터 레코드**
입니다 — 측정 시나리오 이름(`Scooping of coarse dust`), 맥락 설명 문단,
프로젝트 참조번호(`Proj ref: V99.267`). 이것들은 화면 UI 가 아니라 사용자가
"내 시나리오와 유사한가"를 판단해야 하는 **기술 자료**입니다.

번역하지 않았습니다. 이유는 세 가지입니다:

1. 학술 인용을 원문 유지하기로 한 결정과 같은 성격입니다.
2. 라이브러리 전체 규모가 열려 있습니다 — 활동 조합마다 다른 시나리오가 나오므로
   끝이 없습니다.
3. **유사성 판단을 왜곡할 위험**이 있습니다. 평가자가 원문을 그대로 읽고 판단하는
   편이 안전합니다.

따라서 덮임률은 두 가지로 재야 합니다:

| 대상 | 덮임률 |
|---|---|
| 화면 UI (528개) | **96.4%** — 남은 19개는 전부 의도적 원문 유지 |
| UI + 노출DB 내용 (668개) | 77.2% |

앞의 숫자가 프로젝트의 실질 지표입니다. 뒤 숫자는 데이터 레코드를 포함한 값입니다.

### 알려진 소소한 한계 — `N min`

지속시간이 `480 min` 처럼 표시되는데, 숫자가 매번 달라 사전 키로 못 잡습니다.
`min` 과 `0 min` 만 사전에 있습니다. 임의의 숫자까지 처리하려면 정규식 규칙이
필요한데, 현재 설계(정확 일치 + 14자 이상 부분 치환)에는 그런 장치가 없습니다.
숫자와 단위는 원문 유지가 원칙이므로(4절) 그대로 두는 것도 일관된 선택입니다.

### 수집 환경 다시 붙이기

크롬 창을 닫았다면:

```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9222 `
  --user-data-dir="C:\Users\user\.claude-parallel\art-korean\.chrome-debug"
```

이 프로필에는 **ART 로그인이 이미 되어 있습니다.** 평소 쓰는 크롬은 그대로 둬도 됩니다
(`--user-data-dir` 이 다르면 별개 인스턴스로 동시에 뜹니다).

> **기본 프로필로는 안 됩니다.** 크롬 136 부터 기본 사용자 데이터 디렉터리에서는
> `--remote-debugging-port` 가 무시됩니다(쿠키 탈취 대응). 이번에 실제로 확인했습니다.

작업 흐름:

```bash
cd C:\Users\user\.claude-parallel\art-korean\tools
node cdp_collect.js --watch      # 백그라운드 수집 (2초 주기, 새 문자열만 누적)
node drive.js dump               # 현재 화면 구조
node drive.js radios             # 라디오/체크박스 목록 (k 필드가 종류)
node drive.js eval "<js>"        # 임의 JS (이름 없는 요소용)
node autowalk.js 25              # 질문 화면 자동 통과 (첫 선택지를 고름)
python dom_diff.py collected.txt # 사전과 대조
python apply_nearmiss.py         # 표기 차이 자동 반영
```

### 절대 사전에 넣으면 안 되는 것

수집 결과에는 **사용자의 실제 시나리오 이름**(`xylene`, `toluene`, `20260225 PDC연습`)과
로그인 사용자 이름이 섞여 들어옵니다. 화면 문구가 아니라 사용자 데이터입니다.
`art-ko-dict.json` 에 넣지 말고, `collected.txt` 도 그대로 공개하지 마십시오.
(한글 이름은 `collect.js` 가 영문 3자 이상만 담으므로 자동으로 걸러집니다.)

### 도구를 고칠 때 걸리는 함정 세 가지

**1. heredoc 이 백슬래시를 접습니다.** 이 환경의 `<<'EOF'` 는 인용부호를 붙여도
`\\` 를 `\` 로 줄입니다. 파일에 넣고 싶은 백슬래시를 **그대로** 쓰십시오. Python 패치
스크립트는 raw 문자열(`r'...'`)을 쓰고, 애매하면 `BS = chr(92)` 로 조립하는 게 확실합니다.

**2. JS 템플릿 리터럴이 한 번 더 먹습니다.** `drive.js` 는 페이지에서 실행할 코드를
백틱 문자열에 담아 보냅니다. 템플릿 리터럴 안의 `\s` 는 JS 가 `s` 로 해석하므로
정규식 `/\s+/g` 가 **`/s+/g` 가 되어 문자 `s` 를 전부 지웠습니다** (`Close` → `Clo e`).
페이지로 보낼 코드 안에서는 `\\s+` 로 써야 합니다. 증상이 "글자가 하나씩 빠진다" 로
나타나 원인을 짐작하기 어렵습니다.

**3. Git Bash 가 경로를 변환합니다.** `/loggedin/x.aspx` 를 넘기면 MSYS 가
`C:/Program Files/Git/loggedin/x.aspx` 로 바꿉니다. 그대로 URL 에 이어 붙으면 호스트가
`advancedreachtool.comC:` 가 되어 DNS 오류 페이지로 갑니다. `drive.js` 가 자체적으로
되돌리지만, 호출할 때 `MSYS_NO_PATHCONV=1` 을 붙이는 것이 정석입니다.

또 하나: **네트워크 오류 페이지에 갇히면 `location.href` 할당이 먹지 않습니다.**
브라우저 수준 `Page.navigate` 를 써야 빠져나옵니다 (`drive.js nav` 이 그렇게 되어 있습니다).

### 커버리지를 다시 재는 법

```bash
cd tools
grep "^/loggedin/" collected.by-page.tsv | cut -f2- | sort -u > mech.txt
python dom_diff.py mech.txt
```

`collected.by-page.tsv` 는 화면 경로와 문자열을 함께 기록하므로, 정보성 페이지
(과학적 배경·교육·컨소시엄)를 제외하고 평가 도구 화면만 따로 잴 수 있습니다.

---

## 1. 무엇을 하는 프로젝트인가

Advanced REACH Tool (ART) 1.5 — `https://www.advancedreachtool.com` — 의
**화면 언어만** 한국어로 바꿉니다. REACH 하의 Tier 2 흡입 노출 평가 도구인데
UI가 EN/DE/FR/NL만 제공해서 국내 사용자가 쓰기 불편한 것이 문제입니다.

### 반드시 지킬 것

**모델 내부 구조, 알고리즘, 입력값은 절대 건드리지 않습니다.** 한글화 전후로
서버가 받는 폼 데이터가 한 바이트도 달라지면 안 됩니다. 번역은 순수하게 표시
계층에서만 일어나야 합니다. 이게 합격 기준이고, `tools/test.js` 가 이걸 검사합니다.

사이트는 사용자 소유가 아닌 외부 서비스입니다(컨소시엄 운영 ASP.NET WebForms).

---

## 2. 현재 상태

### 산출물

| 파일 | 내용 |
|---|---|
| `art-korean.user.js` | 설치할 Tampermonkey 스크립트 (빌드 결과물, 직접 수정 금지) |
| `art-ko-dict.json` | **사전 원본 555개 항목.** 여기를 고치고 다시 빌드합니다 |
| `README.md` | 설치법, 안전성 근거, 용어 기준, 재추출 절차, 라이선스 주의 |
| `tools/build.py` | 사전 → 유저스크립트 빌드. 충돌·빈 값 검사 후 중단 |
| `tools/test.js` | jsdom 검사 45개 (node_modules 포함, 바로 실행 가능) |
| `tools/art-korean.template.js` | 스크립트 템플릿 (`/*__DICT__*/` 자리에 사전 주입) |
| `tools/cdp_collect.js` | **로그인된 크롬에 CDP 로 붙어 자동 수집.** 가장 편한 경로 |
| `tools/drive.js` | 화면 조종(클릭·입력·선택). **계정에 데이터를 씁니다** |
| `tools/walk.js` | 단순 화면 자동 통과. 판단 지점에서 멈춤 |
| `tools/autowalk.js` | 질문 화면 자동 통과(첫 선택지). **계정에 데이터를 씁니다** |
| `tools/live_check.js` | 실제 페이지에 주입해 폼 직렬화 전후 비교 |
| `tools/live_submit_check.js` | **진짜 클릭으로 POST 본문을 잡아 검사.** 최종 합격 판정 |
| `tools/sweep_activities.js` | 활동 등급 트리 전체 순회 |
| `tools/spelling_variants.py` | 영/미 철자 변형 키 자동 생성 |
| `tools/typography_variants.py` | 대시·도 기호 변형 키 자동 생성 |
| `tools/apply_nearmiss.py` | dom_diff 의 '표기 차이' 를 사전에 자동 반영 |
| `tools/merge_pending.py` | 번역 묶음 JSON 을 사전에 병합(접두사 매칭) |
| `tools/collect.js` | 화면 문자열 수집기. 콘솔 붙여넣기용 (CDP 없이 쓸 때) |
| `tools/dom_diff.py` | **수집 결과 ↔ 사전 대조.** 미번역/표기차이/부분번역으로 분류 |
| `tools/parse3.py` | 4장 → 질문·선택지·경고·예시 추출 |
| `tools/parse_guidance.py` | 4장 → 도움말 추출 (151 → 고유 37) |
| `tools/source/art_mech_v15.pdf` | **주 출처 원본** (374쪽, 로컬 보관) |
| `tools/source/ch4_raw.txt` | 4장 추출 텍스트 (재추출 없이 바로 사용 가능) |
| `_superseded/` | 초기 시제품. 무시하세요 |

빌드·테스트:

```bash
cd C:\Users\user\.claude-parallel\art-korean\tools
python build.py     # → ../art-korean.user.js
node test.js        # → ALL PASS (45 passed, 0 failed)
```

### 완료된 번역 — **4차 세션 종료 시점 1,887개** (아래 표는 2차 세션 기록)

`Gas` 만 `workflow` 와 `options` 양쪽에 같은 번역으로 들어 있어 빌드 시 하나로
합쳐집니다. 충돌이 아니므로 `build.py` 가 통과시킵니다.

| 섹션 | 개수 | 출처 |
|---|---|---|
| `site` | 27 | 실제 DOM 대조 완료 |
| `auth` | 25 | 실제 페이지 관찰 |
| `alerts` | 6 | 실제 페이지 관찰 |
| `workflow` | 54 | ART User Guide |
| `questions` | 68 | 4장 + 실제 DOM |
| `warnings` | 14 | 4장 + 실제 DOM |
| `options` | 366 | 4장 + 실제 DOM |
| `examples` | 114 | 4장 |
| `guidance` | 140 | 4장 + 실제 DOM |
| `scenarios` | 28 | 실제 DOM (myscenarios) |
| `nav` | 19 | 실제 DOM (공통 버튼) |
| `wizard` | 148 | 실제 DOM (마법사) |

**기계론적 모델 입력 과정 전체가 덮였습니다** — 질문, 드롭다운 선택지, 예시 활동,
질문별 도움말까지.

### 남은 것 (우선순위 순)

1. **로그인 후 화면을 실제 DOM 과 대조** ← 지금 가장 가치가 높음.

   로그인 **전** 화면은 2026-08-22 에 대조를 마쳤습니다. 수집된 43개 중 번역
   대상 31개 전량이 덮였고, 나머지 12개는 의도적으로 원문을 유지합니다
   (`ART Standard` 제품명, 언어 전환기 `Deutsche`/`Francais`/`Nederlands`/`English`,
   컨소시엄 약어 `TNO`·`IOM`·`HSL`·`IRAS`·`baua`·`AMI`, ASP.NET 흔적 `__LogIn`).
   대조 전에는 48.8% 였습니다 — **"실제 페이지 관찰로 완료"라고 적혀 있던 구간이
   실제로는 절반이었습니다.** 로그인 후 화면도 같은 정도의 누락을 예상하십시오.

   ### 방법 A — CDP 자동 수집 (권장)

   크롬에 붙어 화면을 읽습니다. 페이지를 조작하지 않으므로 계정에 아무것도
   쓰지 않습니다.

   1. **디버깅 전용 프로필로 크롬을 하나 더 띄웁니다.** 평소 쓰는 크롬은 그대로
      둬도 됩니다 — `--user-data-dir` 이 다르면 별개 인스턴스로 동시에 뜹니다.

      ```powershell
      & "C:\Program Files\Google\Chrome\Application\chrome.exe" `
        --remote-debugging-port=9222 `
        --user-data-dir="C:\Users\user\.claude-parallel\art-korean\.chrome-debug"
      ```

      > **기본 프로필을 쓰면 안 됩니다.** 크롬 136 부터 기본 사용자 데이터
      > 디렉터리에서는 `--remote-debugging-port` 가 무시됩니다(쿠키 탈취 대응).
      > 반드시 별도 `--user-data-dir` 을 지정하십시오. 이 프로필은 유지되므로
      > ART 로그인은 최초 한 번만 하면 됩니다.

   2. 그 창에서 ART 에 로그인하고 대상 화면을 엽니다.
   3. 감시 모드로 수집을 걸어 둡니다:

      ```bash
      cd C:\Users\user\.claude-parallel\art-korean\tools
      node cdp_collect.js --watch
      ```

   4. 크롬에서 화면을 클릭해 돌아다니면 2초마다 새 문자열이 `collected.txt` 에
      쌓입니다. 다 돌았으면 `Ctrl+C`.

   ### 방법 B — 콘솔 붙여넣기 (CDP 를 못 쓸 때)

   대상 화면에서 F12 → Console 에 `tools/collect.js` 를 붙여넣고 Enter.
   클립보드로 복사되므로 `collected.txt` 에 이어 붙입니다. 화면마다 반복.

   ### 대조

   ```bash
   cd C:\Users\user\.claude-parallel\art-korean\tools
   python dom_diff.py collected.txt
   ```

   `dom_diff.py` 는 유저스크립트의 조회 규칙(원문 → 공백 정규화 → 14자 이상 부분
   치환)을 그대로 재현합니다. 여기서 "완전 번역"으로 나오면 실제 화면에서도
   번역됩니다. 결과는 세 갈래입니다:

   | 출력 | 의미 | 할 일 |
   |---|---|---|
   | `art-dom-untranslated.json` | 전혀 안 걸림 | 번역을 새로 작성 |
   | `art-dom-nearmiss.json` | 대소문자·문장부호만 다름 | **사전에 변형 키만 추가** |
   | `art-dom-partial.json` | 일부만 치환되고 영어가 남음 | 남은 조각을 키로 추가 |

   `nearmiss` 가 핵심입니다. 4장은 문장을 문어체 표기로 싣는데 실제 화면은
   `My Scenarios:` 처럼 콜론이 붙거나 대소문자가 달라서, 번역이 있는데도 영어로
   남습니다. **조회 규칙을 느슨하게 풀지 마세요** — 오탐이 늘고 3.4절 부분 치환
   정책과 충돌합니다. 사전에 변형 키를 넣는 쪽이 맞습니다.

   스크립트를 이미 설치했다면 `Alt+C` → 화면 이동 → `Alt+E` 로 내보낸 JSON 을
   `dom_diff.py` 에 그대로 넘겨도 됩니다.

2. **베이지안 모듈 · 측정데이터 가져오기 · 결과 화면 — 거의 비어 있음.**
   현재 `workflow` 섹션의 용어 몇 개(`Fully analogous`, `Full-Shift`, 백분위수 등)뿐입니다.

   이유가 중요합니다: 주 출처인 기계론적 모델 보고서 1.3절이 *"This report will
   focus on the development of the mechanistic model"* 이라고 명시하고, 4장(370쪽)
   다음이 바로 5장 결론입니다. **해당 화면 문자열이 애초에 그 문서에 없습니다.**
   문서를 더 뒤지지 말고 1번(실제 DOM)으로 해결하는 게 빠릅니다.

   문서 경로를 굳이 가려면: McNally et al. 2014 (Ann Occup Hyg 58(5):551-565,
   베이지안), Schinkel et al. 2013 (57(6):717-727, 노출 데이터베이스),
   그리고 `https://www.advancedreachtool.com/assets-1.5.12110.3/doc/` 폴더의
   User Guide / 교육자료. 페이월 가능성 있습니다.

3. **이전 계정의 v1.1 사전(416개) 미병합.** 다른 계정에 `art-korean.user.js` v1.1과
   `art-ch4-untranslated.json` 이 있다는 인계 기록이 있었으나 이 머신에 없었습니다.
   현재 555개가 그 416개를 대체로 포섭할 것으로 보이지만, 접근 가능하면 대조하세요.

4. **검증 메시지·툴팁** — 실사용하며 수집 모드로 보완.

5. (선택) 컨소시엄에 공식 로케일 제안 — `README.md` 라이선스 절 참조.

---

## 3. 반드시 유지해야 하는 기술 결정

다시 구현할 일이 있으면 이 절을 그대로 지키세요. 전부 테스트로 고정돼 있습니다.

### 3.1 `<select>` — value 고정 (value pinning)

WebForms 의 `<select>` 는 선택된 `<option>` 의 `value` **속성**을 전송하는데,
속성이 없으면 브라우저가 **표시 텍스트**를 대신 전송합니다. 그래서 번역 **전에**
암묵적 값을 명시적 속성으로 고정합니다.

```js
if (!opt.hasAttribute('value')) opt.setAttribute('value', opt.value);
opt.textContent = '분말, 과립 또는 펠릿형 재료';
```

HTML 명세상 value 속성이 있으면 전송되는 건 속성값이고 텍스트는 표시 전용이라,
이후 `select.value` 는 계속 영어 원문을 반환합니다. 되돌릴 필요도, submit 을
가로챌 필요도 없습니다. `RequiredFieldValidator` 의 `InitialValue` 비교도 value
기준이라 그대로 통과합니다.

> **순서가 함정입니다.** 이번 세션에서 실제로 잡은 버그입니다. 일반 텍스트 워커가
> option 텍스트를 먼저 바꾸면, 뒤이은 value 고정이 *한국어* 값을 고정해 전송
> 데이터가 바뀝니다. 그래서 `SKIP_TAGS` 에 `OPTION`/`OPTGROUP` 을 넣어 텍스트
> 워커에서 제외하고, `apply()` 안에서 `doOptions()` 를 **가장 먼저** 실행합니다.

### 3.2 `<input type="submit">` — 전송 직전 원문 복원, 네 경로 전부

버튼은 표시와 전송이 같은 `value` 속성이라 분리가 불가능합니다. 전송 직전
복원하되 **네 경로를 모두** 막습니다.

1. `pointerdown` / `keydown` (캡처 단계)
2. `submit` 이벤트 (캡처 단계)
3. `HTMLFormElement.prototype.submit` 패치 ← **여기가 함정**
4. `PageRequestManager.add_initializeRequest`

**3번이 중요한 이유:** ASP.NET 의 `__doPostBack()` 은 `form.submit()` 을 코드로
호출하는데, 이 경우 브라우저는 submit 이벤트를 발생시키지 않습니다. 제품 유형을
고르면 Activity Class 목록이 갱신되는 그 동작이 바로 이 경로입니다. submit
리스너만 달면 **가장 자주 쓰는 길에서 안전장치가 안 걸립니다.**

프로토타입 패치는 `@grant none` 이어야 동작합니다. `GM_*` 권한을 쓰면 샌드박스로
분리돼 페이지의 프로토타입에 닿지 못합니다.

### 3.3 공백 정규화 조회

HTML 은 긴 문장을 줄바꿈과 연속 공백으로 감싸 놓습니다. 사전 조회는 원문 그대로
한 번, 실패하면 공백을 하나로 접은 형태로 한 번 더 시도합니다(`NORM` 인덱스).
**이게 없으면 긴 질문문과 도움말 37개는 실제 DOM 에서 거의 매칭되지 않습니다.**

### 3.4 부분 치환은 14자 이상만

짧은 단어까지 부분 치환하면 단어 중간에 오탐이 생깁니다(`Gas`, `Metal`, `Wood`
같은 항목). `PHRASE_MIN = 14`, 긴 키부터 적용.

### 3.5 안전한 폴백

사전에 없는 문구는 **영어 원문 그대로** 남깁니다. 틀린 번역보다 안전합니다.

---

## 4. 용어 통일 기준

전체는 `art-ko-dict.json` 의 `_meta.glossary` 에 있습니다. 화면에서 하나씩
주워담으면 용어가 갈리므로 이 표를 우선하세요.

| 원문 | 번역 | 비고 |
|---|---|---|
| Segregation | 격리(발생원) | 발생원을 가둠 |
| Separation / Personal enclosure | 격리(작업자) | 작업자를 가둠. **위와 구분 필수** |
| Localized controls | 국소 제어 | |
| Local exhaust ventilation | 국소배기장치 | |
| Dispersion / Dilution | 확산 / 희석 | |
| Near field / Far field | 근거리장 / 원거리장 | |
| Dustiness | 발진성 | KOSHA 표준 용어 |
| Fracturing / Crushing / Abrasion / Impaction | 파쇄 / 분쇄 / 마모 / 충격 | |
| Agitation | 교반 | |
| Modifying factor | 보정인자 | |
| Emission potential | 배출 잠재력 | |
| Surface contamination | 표면 오염 | |
| RPE | 호흡보호구 | |
| Full-Shift / Long-Term Average | 전 교대시간 / 장기 평균 | |
| Percentile | 백분위수 | |
| weight fraction / mole fraction | 질량분율 / 몰분율 | |

**숫자와 단위는 원문 유지** (`> 3 l/minute`, `100 - 1000 kg`, `0.3 ACH`).

---

## 5. 4장 재추출 시 함정 (필요할 경우만)

`tools/source/ch4_raw.txt` 가 이미 있으니 보통 재추출은 불필요합니다.
다시 뽑아야 하면:

```bash
pdftotext -f 177 -l 370 -enc UTF-8 tools/source/art_mech_v15.pdf ch4_raw.txt
python tools/parse3.py          # 질문·선택지·경고·예시
python tools/parse_guidance.py  # 도움말
```

- **`-layout` 을 쓰지 마세요.** 표의 열을 질문 텍스트와 같은 물리적 줄에 병합시켜
  `Assigned What is the viscosity...? value` 같은 쓰레기를 만듭니다. 기본 모드는
  질문 한 줄, 선택지 한 줄로 깔끔합니다. (이전 인계 문서는 `-layout` 을 권했는데,
  그게 그 문서가 말한 "함정 3"의 *원인*이었습니다.)
- 선택지 계층이 4단계: `U+F0B7` / `o ` / `U+F0A7` / `- `
- `- ` 를 무조건 마커로 보면 `0.1 - 1 kg/minute` 같은 **수치 범위가 잘립니다.**
  대문자·`(` 가 뒤따를 때만 마커로 취급.
- **사용자영역(PUA, U+E000–U+F8FF) 문자를 반드시 제거하세요.** Wingdings 불릿과
  깨진 수식 기호가 눈에 보이지 않게 키 끝에 남아 매칭을 깨뜨립니다. 이번 세션에서
  `...surfaces. \uf0b7` 때문에 테스트가 실패해서 발견했습니다.
- 페이지 머리말(`TNO report | V9009`, `Chapter 4:`, `181 / 374`) 제거.
- `-> go to question 15.5.1` 류 분기 안내와 `[...]` 저작 메모는 UI에 안 나옴.
- `Question N:` 헤딩이 줄 중간에 나타나므로 별도 줄로 분리.
- `Answer:` 마커가 없는 블록도 있어 본문 불릿 구간을 폴백 파싱.
- 도움말은 NF(4.4.x)/FF(4.16.x)가 거울처럼 반복돼 **151개 → 고유 37개**(75% 감소).

---

## 6. 알려진 위험 (미검증)

**로그인 후 페이지의 자체 JS 가 option 의 *텍스트*를 읽어 분기한다면**
(`$('#ddl option:selected').text()` 같은 코드) 번역 때문에 그 분기가 틀어질 수
있습니다. 전송 데이터는 그대로지만 화면 흐름이 어긋날 수 있습니다.
계정 없이 검증하지 못했습니다. 이상하면 `Alt+K` 로 즉시 영어 복귀.

**진행 중인 실제 평가에 스크립트를 얹지 마세요.** 먼저 버릴 시나리오로 확인하세요.

브라우저 자동번역과 동시에 켜면 충돌합니다. 자동번역은 꺼두세요.

---

## 7. 라이선스 — 공개 배포 전 확인 필요

ART 는 무료지만 오픈소스가 **아닙니다.** TNO 보고서 표지에 "TNO 의 사전 서면 동의
없이 어떤 방식으로도 복제·공개 불가"라고 명시돼 있고, 웹 앱도 컨소시엄이 운영하는
비공개 애플리케이션입니다.

개인 사용은 문제없습니다. UI 문자열 전량 번역본을 공개 배포하는 것은 법적으로
명확하지 않습니다. 공익 목적 배포를 원하면 **컨소시엄에 제안하는 쪽이 낫습니다** —
사이트에 이미 EN/DE/FR/NL 전환이 있어 서버에 언어별 리소스 파일이 존재하고,
한국어 추가는 파일 하나 더 넣는 일입니다. 승인되면 설치 없이 모든 한국어
사용자에게 닿고 라이선스 문제도 소멸합니다. 연락처 `ART@hse.gov.uk`.
컨소시엄은 TNO·IOM·BAuA·NRCWE·IRAS·HSL 로 전부 학술/공공기관입니다.

`art-ko-dict.json` 이 그대로 제안용 번역 대조표가 됩니다.

---

## 8. 출처

| 문서 | 위치 | 용도 |
|---|---|---|
| ART Mechanistic model report v1.5 (2013-01-18), 374쪽 | `tools/source/art_mech_v15.pdf` | **주 출처.** 4장(177~370쪽)에 기계론적 모델 UI 전량 |
| 4장 추출 텍스트 | `tools/source/ch4_raw.txt` | 재추출 없이 바로 사용 |
| ART User Guide 1 | `https://www.advancedreachtool.com/assets-1.5.12110.3/doc/ART_User_Guide_1.doc` | 모델 흐름, 베이지안·결과 화면 용어 일부 |

`Fransman_Development_of_a_mechanistic_model_for_ART.pdf` 도 사이트에 있지만
**2010년 v1.0판**입니다. 1~2장은 같지만 3~4장은 다를 수 있으니 v1.5판을 쓰세요.

---

## 9. 이번 세션에서 실제로 잡은 것 (재발 방지용)

1. **option 순서 버그** — 텍스트 워커가 option 을 먼저 번역해 value 고정이 한국어
   값을 박을 수 있었음. 전송 데이터가 바뀌는 치명적 결함. (3.1 참조)
2. **PUA 문자 잔류** — 보이지 않는 `\uf0b7` 가 키 끝에 남아 매칭 실패. 테스트가
   잡아줬습니다. (5 참조)
3. **공백 정규화 부재** — 긴 문장이 실제 DOM 에서 매칭 안 되는 문제. (3.3 참조)
4. **`-layout` 이 문제의 원인** — 이전 인계 문서의 권고를 뒤집었습니다. (5 참조)
5. **도움말 75% 중복** — 48,000자로 보였던 분량이 실제 15,000자.
6. **"관찰로 완료"가 실제로는 절반** — 로그인 전 화면을 헤드리스 크롬으로 실제
   수집해 대조하니 43개 중 21개(48.8%)만 덮여 있었습니다. 홈페이지 본문 3개,
   `Please wait...`/`Loading...` 같은 상태 문구, `alt` 속성이 통째로 빠져 있었습니다.
   10개를 추가해 번역 대상 전량을 채웠습니다. **눈으로 본 것과 DOM 은 다릅니다.**
7. **수집 범위 누락** — 이전 인계의 콘솔 스니펫은 `title`/`placeholder`/`alt` 속성과
   `input[type=submit]` 의 `value` 를 안 봤습니다. 유저스크립트는 그것들도 번역하므로
   스니펫으로는 "다 됐다"고 나와도 실제로는 영어로 남는 구간이 생깁니다.
   `collect.js` 는 유저스크립트와 같은 범위를 봅니다.
